# 02 — 上线范围与发布门槛

## 范围理念

本文件曾用于 MVP 范围冻结，文件名暂时保留以避免文档链接失效。当前项目目标已经切换为**可上线试点的 iOS 客户端**，范围决策遵循以下原则：

1. **真实闭环优先**：预约、接单、状态流转、取消、紧急事件必须接入真实后端并在真机验收。
2. **Mock 只用于开发测试**：Mock 可用于离线 UI 和单元测试，但不能作为上线验收依据。
3. **无障碍不可妥协**：VoiceOver、TTS、极简大按钮必须在每一个盲人端页面上正常工作。
4. **一个手机号可拥有双角色**：用户可以在 App 内切换 activeRole，但有活跃订单时禁止切换。
5. **上线风险显式记录**：固定验证码和 HTTP 已允许用于当前版本；UserDefaults token、隐私合规和双真机验收状态必须进入 `plan.md`。

---

## 在范围内 (In-Scope)

### 通用模块（双角色共享）

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 手机号 + 验证码登录 | P0 | 验证码固定 `000000`，首次登录自动创建账号 |
| JWT Token 管理 | P0 | 登录返回 JWT，存储于 UserDefaults |
| 角色选择与切换 | P0 | 支持在盲人跑者 / 志愿者之间切换 |
| 角色切换拦截 | P0 | 存在 PENDING_ACCEPT / DRIVER_EN_ROUTE / DRIVER_ARRIVED / IN_PROGRESS 状态订单时禁止切换 |
| API 环境切换 | P1 | Debug 开发包支持 Mock / Demo Cloud；Demo/Production 包隐藏切换入口 |
| 设置页 | P2 | 基本设置：个人信息、关于、退出登录 |

### 盲人跑者端

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 盲人资料填写 | P0 | 必填：昵称、紧急联系人姓名、紧急联系人电话；选填：跑步经验 |
| 盲人首页 | P0 | 语音优先呈现当前状态和下一步主操作；地图定位保留为辅助确认内容 |
| 创建预约 | P0 | 引导式流程：确认出发地点、选择预约时间、填写选填跑步需求、确认后提交；选填项包括路线备注、预计时长、配速偏好、路线偏好、导盲犬和特殊说明 |
| 订单状态等待页 | P0 | WebSocket 接收状态变化，断开时每 5 秒轮询订单详情 |
| 盲人服务中页面 | P0 | 显示志愿者信息和服务进行状态；当前 release 隐藏一键求助入口 |
| 盲人完成/评分页 | P0 | 显示完成状态，星级评分 UI（不强制提交） |
| 取消订单 | P0 | 盲人可在 PENDING_MATCH / PENDING_ACCEPT / REMATCHING 取消；志愿者取消后订单进入 REMATCHING，盲人端可用盲人 token 取消，均需二次确认 |
| 重复当前状态播报按钮 | P0 | 每个关键页面提供，与 VoiceOver 并存不冲突 |

### 志愿者端

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 志愿者资料 + 实名认证 | P1 | 填写昵称，提交身份证、人脸核验，管理员审核通过后可接单 |
| 志愿者首页 | P0 | 系统派单工作台、可服务开关、覆盖范围、当前订单、积分/统计信息 |
| 订单列表页 | P2 | 非主流程旧入口；系统派单上线后不作为志愿者主动选单体验 |
| 订单详情页 | P0 | 接受系统派单后的订单完整信息与服务操作 |
| 志愿者服务中页面 | P0 | 前往出发地点地图固定红色出发点标记、外部地图步行导航、「我已出发」「我已到达」「开始服务」按钮、志愿者在 PENDING_ACCEPT / DRIVER_EN_ROUTE / DRIVER_ARRIVED / IN_PROGRESS 可取消，进入 IN_PROGRESS 后显示「结束服务」 |
| 服务记录页 | P1 | 已完成的订单列表，显示时间、盲人昵称、积分 |
| 积分/商城页 | P2 | 当前展示积分余额和商品入口；真实兑换、库存和支付按后续契约接入 |
| 一键求助 | P0 | 后端合同保留，当前 release 不显示入口；后续安全专项启用时必须补 GPS、通知、失败提示、合规文案和验收 |
| 取消订单 | P0 | 盲人可在 PENDING_MATCH / PENDING_ACCEPT / REMATCHING 取消；志愿者可在 PENDING_ACCEPT / DRIVER_EN_ROUTE / DRIVER_ARRIVED / IN_PROGRESS 取消，志愿者取消后进入 REMATCHING；均需二次确认 |

### 地图与定位

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 高德地图显示 | P0 | 展示地图、当前位置、订单地点 marker；盲人端地图为辅助确认，不能先于状态、主操作和重复当前状态 |
| 当前定位获取 | P0 | 使用 AMapLocation SDK |
| 禁定位拦截 | P0 | 拒绝定位权限时，盲人不能创建预约；志愿者不能接收或接受依赖位置的系统派单 |
| 距离计算 | P0 | iOS 端可展示距离；系统派单排序、扩圈和兜底由后端控制 |
| 定位权限提示 | P0 | 引导用户开启定位权限 |
| 默认测试坐标 | P1 | 模拟器定位失败时提供默认测试坐标 |

### 语音与无障碍

| 功能 | 优先级 | 说明 |
|------|--------|------|
| VoiceOver 支持 | P0 | 所有盲人端关键按钮、输入框、状态文本配置 accessibilityLabel / accessibilityHint |
| TTS 语音播报 | P0 | AVSpeechSynthesizer，所有关键流程节点播报 |
| 语音输入 | P1 | Speech Framework，用于地点描述、备注等文本输入 |
| 极简大按钮 | P0 | 盲人端关键主按钮最小高度 64pt |
| 危险操作二次确认 | P0 | 取消订单、结束服务、退出登录；未来恢复一键求助时也必须二次确认 |

### 外部云端 API 对接

| 模块 | 优先级 | 说明 |
|------|--------|------|
| auth（登录认证） | P0 | 手机号 + 验证码登录，返回 JWT |
| user（用户管理） | P0 | 用户创建、角色管理 |
| profile（资料管理） | P0 | 盲人/志愿者资料 CRUD |
| order（订单管理） | P0 | 订单 CRUD、状态流转、WebSocket 通知与轮询降级查询 |
| location（位置） | P1 | 位置信息存储 |
| safety（安全） | P0 | 紧急联系人存储；紧急求助触发仅保留后端合同探针，当前 iOS release 隐藏入口 |
| volunteer（志愿者） | P0 | 认证状态管理、可服务开关 |
| OpenAPI 契约 | P0 | 以 `docs/07-api-contract.openapi.yaml` 驱动前端对接 |
| 并发接单错误处理 | P0 | 前端正确处理 `ORDER_ALREADY_ACCEPTED` |

以上均为 iOS 消费的外部服务能力。本仓库不实现或部署这些服务端模块。

---

## 路线图能力

以下能力不再作为“禁止项”处理。它们属于上线版路线图，必须先补产品规则、后端契约、隐私/合规评估和自动化/人工验收方案，再进入实现：

| 能力 | 进入条件 |
|------|----------|
| Android 客户端 | 独立仓库或明确扩大仓库边界 |
| 管理员后台 / 审核工作台 | 后端与管理端契约明确 |
| 真实短信服务 | 后端提供验证码策略、限流、错误码和测试账号机制 |
| 真实实名认证 | 身份服务、隐私合规和审核规则明确 |
| 实时轨迹共享 / 路线导航 | 地图能力、位置频率、隐私授权和安全策略明确 |
| 自动拨打电话 / 发送短信 / APNs | 用户授权、合规文本和后端通知契约明确 |
| AI 助手 / 自然语言时间解析 | 误识别兜底、安全边界和无障碍交互明确 |
| 完整积分商城 / 支付 / 库存 | 商品、订单、支付、退款和风控契约明确 |
| App 内聊天 / 虚拟号码 | 消息、电话保护、审核与安全规则明确 |
| 风控 / 摔倒检测 / 电子围栏 | 传感器、误报、告警责任和隐私规则明确 |
| 即时呼叫 / 多人活动报名 | 产品流程、派单策略和并发状态机明确 |

---

## 技术栈决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| UI 框架 | SwiftUI | 开发速度快，MVVM 天然适配 |
| 架构 | MVVM | 简单性与可测试性平衡 |
| 地图 | 高德地图 iOS SDK | 需求指定，非 Apple MapKit |
| 网络层 | URLSession + async/await | 原生，无额外依赖 |
| Token 存储 | UserDefaults | 当前实现；上线硬化优先迁移 Keychain |
| 实时更新 | WebSocket + 5 秒轮询降级 | 云端契约要求 WebSocket，轮询用于断线兜底 |
| 外部 API | `http://47.114.113.171` | 唯一真实联调服务，不由本仓库维护 |
| 认证 | JWT | 无状态，移动端友好 |

---

## 环境配置

### 两种 API 模式

| 模式 | 说明 | 使用场景 |
|------|------|----------|
| Mock | 本地假数据，无后端 | UI 和流程调试，不能用于上线验收 |
| Demo Cloud | `http://47.114.113.171` | 所有真实网络联调；Demo/Production 包固定使用 |

### 高德地图 Key 管理

- Key 存储在 `LocalConfig.xcconfig` 或本地 plist 中。
- 本地配置文件加入 `.gitignore`，不提交 Git。
- 项目提供 `LocalConfig.example.xcconfig` 说明需要哪些 key。

---

## Demo 演示脚本

### 场景 1：完整正向流程（Happy Path）

| 步骤 | 操作 | 设备 | 预期结果 |
|------|------|------|----------|
| 1 | 盲人登录（手机号 + 000000） | 手机 A | 进入角色选择页 |
| 2 | 盲人选择"盲人跑者"角色 | 手机 A | 进入盲人资料页（首次） |
| 3 | 盲人填写资料并提交 | 手机 A | 进入盲人首页 |
| 4 | 盲人点击"开始约跑" | 手机 A | 进入创建预约页 |
| 5 | 盲人选择出发地点、预约时间，提交 | 手机 A | 进入订单等待页，TTS 播报"订单已提交" |
| 6 | 志愿者登录 | 手机 B | 进入角色选择页 |
| 7 | 志愿者选择"志愿者"角色 | 手机 B | 进入志愿者认证页（首次） |
| 8 | 志愿者完成身份证、人脸核验并通过管理员审核 | 手机 B | 进入志愿者首页，看到订单 |
| 9 | 志愿者点击订单，查看详情 | 手机 B | 显示订单详情 |
| 10 | 志愿者点击"接单" | 手机 B | 订单状态变为 PENDING_ACCEPT |
| 11 | 盲人端轮询到状态变更 | 手机 A | 页面更新，TTS 播报"志愿者已接单" |
| 12 | 志愿者点击"我已出发" | 手机 B | 订单状态变为 DRIVER_EN_ROUTE |
| 13 | 志愿者点击"我已到达" | 手机 B | 订单状态进入 DRIVER_ARRIVED；盲人端提示等待志愿者开始服务 |
| 14 | 志愿者点击"开始服务" | 手机 B | 调用 `POST /api/orders/{id}/start-service`，订单状态进入 IN_PROGRESS，盲人端播报服务开始 |
| 15 | 志愿者点击"结束服务" | 手机 B | 订单状态变为 COMPLETED |
| 16 | 盲人端轮询到状态变更 | 手机 A | 显示完成页面 + 评分 UI，TTS 播报"服务已完成" |
| 17 | 盲人评分（可选） | 手机 A | 完成评分，返回首页 |

### 场景 2：紧急求助当前 release 处理

| 步骤 | 操作 | 设备 | 预期结果 |
|------|------|------|----------|
| 1 | 订单处于 DRIVER_EN_ROUTE / DRIVER_ARRIVED / IN_PROGRESS 状态 | 双设备 | iOS UI 不显示"紧急求助"或"一键求助"入口 |
| 2 | 后端合同探针调用 `POST /api/emergency/trigger` | 脚本 | 可验证云端 emergency event 合同，不代表 iOS UI 已上线求助 |
| 3 | 后续安全专项恢复求助 | 双设备 | 必须新增 GPS、通知、失败提示、合规文案和真实盲人验收 |

### 场景 3：角色切换拦截

| 步骤 | 操作 | 设备 | 预期结果 |
|------|------|------|----------|
| 1 | 志愿者接单后（PENDING_ACCEPT 状态） | 手机 B | — |
| 2 | 志愿者尝试切换角色 | 手机 B | 弹窗提示"您有进行中的订单，无法切换角色" |
| 3 | 确认提示 | 手机 B | 保持在当前角色页面 |

---

## 上线验收计划

| 阶段 | 重点 | 产出 |
|------|------|------|
| 集成冻结 | 文档、API 契约、测试账号、高德 key、真机 `111` 和 `iPad Pro (2)` 准备 | `plan.md` 风险清单和验证命令 |
| 真实联调 | 高德、定位、登录、订单、WebSocket、紧急事件 | `scripts/production-readiness-check.sh` 结果 |
| 发布硬化 | Keychain、HTTPS、隐私权限文案、错误处理、无障碍验收 | 上线风险关闭或人工确认 |
| 试点发布 | TestFlight / 企业分发 / 线下试点 | 真实用户反馈和问题列表 |

---

## 已知上线风险

1. 预置测试账号验证码固定为 `000000`，当前允许用于上线验收。
2. 志愿者身份证、人脸核验和管理员审核已按后端契约接入，仍需双真机完整走查。
3. GPS 使用设备当前位置，不展示另一方实时位置。
4. 积分当前以展示为主，真实兑换、库存和支付需后续契约。
5. 云端服务由外部团队维护；可用性和数据生命周期不由本仓库控制。
6. 定位依赖设备 GPS / 模拟器模拟定位，室内可能信号不佳。
7. 仅支持 iOS 16+，不支持 Android。
8. HTTP 明文 IP 当前允许上线；UserDefaults token 后续迁移 Keychain。
