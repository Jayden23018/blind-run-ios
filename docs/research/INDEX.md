# 调研索引

本仓库**所有**联网调研的唯一落脚点。规则只有三条：

1. **开搜前整份读这个文件**，不要 `ls | grep` —— `ls` 只匹配文件名，搜「地图」命中不了
   `amap-geocoding-20260801.md`；能搜到的词在下面「问题」和「一句话结论」两列里。
2. 已有结论还作不作数，看「复核触发条件」那列。**没触发就直接用，不要重跑。**
   触发了、或表里根本没有这个问题、或有但缺你要的那一段 —— 才开新一轮。
3. 调研完落盘到 `docs/research/{topic}-{YYYYMMDD}.md`，**并回写下面一行**。
   不回写等于没做：下次搜不到，同一份调研会被原样重跑一遍。

被否掉的方案同样留一行 —— 「试过 X 因为 Y 放弃」跟「选了 Z」一样值钱，且更容易被忘。

强制在 `scripts/hooks/research-log.mjs`：联网工具调用前把本文件灌回给模型（第 1 条），
停止前检查本轮联网过但 `docs/research/` 没动（第 3 条）。自测 `scripts/validate-research-log.mjs`。

| 日期 | 问题 | 一句话结论 | 复核触发条件 | 报告 |
|------|------|-----------|-------------|------|
| 2026-08-05 | 视障用户语音预约陪跑的界面与交互标准：决策点数、尾静音时长、确认词白名单、有订单时首页要不要被接管 | 减法方向对但论证支柱错；Magic Tap 不可依赖（iOS 26 起可整个关闭）；确认词白名单必须重做（Portland 事件证据链）；12 秒尾静音超 Nielsen 10 秒上限要改；订单卡应作第 1 个 VoiceOver 焦点而非整页接管；`YD/T 4211-2023` 引错了，它是域名隐私标准 | iOS 大版本改 VoiceOver 手势或 Magic Tap 语义；`Speech` 框架端点检测默认值变化；国标/行标出新版无障碍要求 | [blind-voice-booking-ia-20260805.md](./blind-voice-booking-ia-20260805.md) |
| 2026-08-12 | `dynamicTypeSize(...accessibility3)` 这样的封顶够不够 WCAG 1.4.4 的「放大 200% 不裁切」？ | **够** —— body 在 AX3 是 40pt / 默认 17pt ≈ **235%**，AX5 是 53pt ≈ 312%。此前 review 初稿凭印象写成「AX3 约 175%，够不到 200%」，是错的。封顶仍该去掉，但理由换成「AX4/AX5 正是低视力用户实际会设的档位」+「全仓只有一个组件封顶，同屏缩放不一致比两个极端都差」。附带：不同文本样式缩放速率不同（AX5 档 Body 比 Title 1 还大），不能拿一个样式的比例推另一个 | Apple 改 Dynamic Type 字号表或新增档位；WCAG 改 1.4.4 的倍数要求 | [dynamic-type-scale-20260812.md](./dynamic-type-scale-20260812.md) |
| 2026-08-12 | 市面同类盲人产品各做了哪些功能？一个盲人 App「必须」有哪些能力与适配，强制性从哪来？ | 同类产品分**三类**（即时视觉协助 / 长期配对陪跑 / 无障碍派单出行），功能集不可互抄；AidRun 是跨类产品 —— 用第三类的派单机制承载第二类的人身安全风险面，缺口要按第二类对；可直接照搬的只有滴滴的三节点提醒 + 勋章激励、UIS 的出发前四要素约定 + 一对多储备池（每人需 6–8 名固定陪跑员）、关怀版的预存常用地址；**面向 App 的无障碍国标目前不存在**，实际依据是 GB/T 37668-2019 + 《移动互联网应用（APP）适老化通用设计规范》，信息无障碍标识两年一检 | 工信部《移动互联网应用程序适老化技术规范》正式出台；WCAG 出新版或 EAA 执行口径变化；对标产品（滴滴无障碍出行 / United In Stride / Be My Eyes）发布重大功能改版 | [blind-app-feature-landscape-20260812.md](./blind-app-feature-landscape-20260812.md) |
| 2026-08-12 | 跑完的轨迹回放页怎么做：地图占多大、统计怎么排、我们缺哪块 | 同类产品只有一种版式——地图占顶部 35–45% + 下面卡片装统计，统计是 3 列网格（数字大标签小在下）而非我们现在的左右对齐行；列表内嵌用压缩版、点进去才是大屏。我们不缺渲染能力也不缺后端数据，缺的是：志愿者「近期服务」进的那个详情页压根没挂轨迹组件（「服务记录」进的那个挂了）、盲人端没有历史订单列表、地图写死 220pt 且无起终点标记 | 悦跑圈/Strava 活动详情页重大改版；后端给 `TrackPointDto` 加逐点配速或海拔（配速染色的否决理由随之消失）；再动盲人端首页信息架构 | [run-track-replay-ui-20260812.md](./run-track-replay-ui-20260812.md) |
| 2026-08-13 | 怎么在腾讯会议上同时演示两台 iOS 真机（盲人端 + 志愿者端）？ | 腾讯会议**没有**「手机投屏当共享源」，只能桌面/窗口/白板；正解是 QuickTime「新建影片录制」开**两个窗口**分别镜像两台 USB 设备，再用腾讯会议 3.30+ 的 Shift 多选窗口共享（本机 3.41.1 满足）；AirPlay 到 Mac 只能救一台、不支持双设备并存，仅作退路；⛔ **iPhone 镜像（iPhone Mirroring）出局** —— 镜像期间 iPhone 必须锁定、麦克风不可用，语音下单跑不起来；QuickTime 的 Microphone 要选「无」不要选 iPhone（会劫持音频路由）；⚠️ 排查「QuickTime 看不到设备」时有**两个会给假阴性的检查**：`system_profiler SPUSBDataType` 在本机 returncode=0 但输出 0 字节，据此判「没插 USB」是错的（真实判据用 `ioreg -p IOUSB` 或 `devicectl device info details` 的 `transportType`）；未签名 CLI 枚举 `AVCaptureDevice` 的 muxed 类别也可能拿不全，不能证伪 QuickTime。macOS 26 **未移除**该功能。设备在下拉的 **Camera** 分组（不是 Screen），且**手机必须解锁**、QuickTime 要在插线之后重开；⚠️ **一个 QuickTime 实例只能开一个影片录制窗口**，第二台设备要 `open -n -a` 起第二个实例（Finder「复制 App」的老办法 Ventura 起已失效）；`Finder 能看到 ≠ QuickTime 能看到`；⚠️ QuickTime 镜像有中途断线且**只能重启 Mac** 恢复的已知 bug，直播演示必须备录屏 | 腾讯会议新增移动设备作为共享源；macOS 大版本改 QuickTime 设备捕获行为或修掉镜像中断 bug；演示设备组合变化 | [tencent-meeting-two-ios-device-demo-20260813.md](./tencent-meeting-two-ios-device-demo-20260813.md) |
| 2026-08-08 | 盲人端首页与接单后页面的视觉规格：主按钮多大、圆形还是圆角矩形、次级操作并排还是堆叠 | 全宽圆角矩形，主按钮占内容区约 75%（现状 `minHeight: 64` 差一个数量级）；圆形是错方向（同尺寸只有 π/4 ≈ 78.5% 可点面积）；次级操作一律整行铺满竖直堆叠，对标产品无一并排；接单后那页要从 7 个 section 减到一张头像加一个按钮 | `docs/ui/reference-screenshots/` 下的对标产品发布重大改版；我们改动首页或接单后页面的信息架构 | [blind-ui-visual-benchmark-20260808.md](./blind-ui-visual-benchmark-20260808.md) |
| 2026-08-14 | App 自己的 TTS 播报能不能跟随用户的 VoiceOver 语速？（review §5.6 / §6.1 #18 排进「要产品拍板」的那条） | **不需要拍板，平台已给答案**：读不到也不该读用户语速（无公开 API，且旁白与朗读内容是两个独立滑块），改设 `AVSpeechUtterance.prefersAssistiveTechnologySettings = true`，VoiceOver 在跑时系统用用户的音色与语速覆盖，没跑时才用我们的值 ⇒ `voice`/`rate` 两行必须保留。`API_AVAILABLE(ios(14.0))`，部署目标 16.0 **不需要 `#available`**；⚠️ WebSearch 转述成「iOS 16+」是错的，版本敏感 API 以本机 SDK 头文件为准。出厂值实测为 `false`（缺陷真实存在），但头文件写明查询属性反映不出用户设置 ⇒ **语速在进程里测不出来，单测只能断言开关，听感必须真机人耳验**。附带：`rate` 标度非线性、中点 0.5 不是 1.0。**未解决**：VoiceOver 开着时同一句同时走通告与合成器是否「念两遍」，仍需真机听 | Apple 改 `prefersAssistiveTechnologySettings` 语义或废弃它；本仓库部署目标降到 iOS 14 以下；两条播报通道（通告 + 合成器）的取舍被重新讨论 | [tts-rate-follows-voiceover-20260814.md](./tts-rate-follows-voiceover-20260814.md) |
| 2026-08-13 | 现有联网搜索/抓取配置能不能覆盖竞品调研 / 医学论文 / 政策文件汇报三类场景？该不该加 Exa、Tavily？什么时候派 subagent 抓、什么时候自己抓？ | **不缺工具，缺免费额度和原文通道**。Firecrawl 是 keyless 层且**本轮实测已撞满当日配额**（`free daily limit for this network has been reached`，约 15.9h 恢复）—— 记忆 `firecrawl-mcp-setup` 写的注册触发条件已到。三场景各自的通道已实测钉死：竞品评价走 **HN Algolia API**（免 key，返回原始评论，是识破厂商软文的唯一途径 —— 实测发现 Firecrawl 在买软文）；论文**不要抓网页**，OpenAlex / Europe PMC / PubMed E-utilities 免 key 实测全 200，Semantic Scholar 无 key 已 429；政策文件 **gov.cn 直连即可**（200 / 0.17s），`r.jina.ai` 转 markdown 再省 64%。**Exa / Tavily 暂不上** —— 独立基准（AIMultiple）显示前四名质量差在噪声内，差 20× 的是延迟。Reddit 三条路全堵（`.json` 403 / 内置浏览器策略拦 / firecrawl 限流）。**§9 追加：官方 OAuth 也不是「注册即用」** —— Responsible Builder Policy 要求事先批准，研究用途必须走 RFR 项目、商业用途需书面批准，且把非商业抓取也列入禁止；本仓库的调研用法落在灰色偏违规区，脚本已写好但**暂停使用**，改为 HN Algolia + 人工阅读（这是「端点存在 ≠ 端点可用」的又一例：第一轮只读了限速那节没读准入那节）。subagent 判据改为**按输出物切而非按任务大小切**：要结论就派，要原话/数字/字段就自己抓（原文经过 subagent 已经是转述，进程边界决定，prompt 修不了）。**§8 追加**：`WebSearch` 要定向搜某站必须用 `allowed_domains` **参数**，写进查询串的 `site:` 不生效且会静默退化成 SEO 软文（本报告第一轮据此误判内置搜索没能力）；`reddit.com` 是 Anthropic user agent 层硬屏蔽（400 硬错误）；`s.jina.ai`≠`r.jina.ai`，前者无 key 直接 401，免费 key 解锁的是整个 search 端点；**Exa 不买** —— 其 ToS §4.2(a) 逐字禁止 download/copy/reproduce 任何经服务获得的信息（仅允许浏览器临时缓存），与本仓库「调研必须落盘归档」直接冲突，§1(c) 另授予永久不可撤销许可且 query 用于训练模型（Firecrawl/Perplexity/Linkup/Tavily 条款同样贪，Jina Reader 暴露面不同因为给它的是 URL 不是研究问题） | Firecrawl 或 Jina 改免费层额度；Reddit 改 Data API 政策；gov.cn 加反爬或改站点结构；Exa 改 ToS §4.2；出现有独立第三方基准支撑的新搜索 API | [web-research-toolchain-20260813.md](./web-research-toolchain-20260813.md) |
| 2026-08-13 | 「行程实时分享给紧急联系人」与「志愿者激励体系」，同类产品的 UI 与架构怎么做？iOS 侧有哪些平台约束？ | 实时分享在 Strava Beacon 与 Uber 上是**同一套架构**：服务端发 token + 免登录只读页，客户端只负责把链接塞进**预填短信由用户手动点发送**，有效期绑行程生命周期而非固定 TTL，联系人上限 3–5 人 ⇒ **实时分享必须先有后端契约，纯前端最多做到静态行程告知短信**；激励可抄「按服务量分层（25/50/100/250/500）+ 徽章视觉差 + 进度常驻可见」，要避开排行榜竞争且必须可退出；⚠️ `MFMessageComposeViewController` 的 `.sent` **不保证送达**、预填可被用户改、系统 composer 是 out-of-process 无法注入无障碍标签（播报只能自己在回调里做）。缺口：服务时长**证书**生成没搜到可用材料 | 后端上线分享令牌/只读轨迹契约；Apple 改 MessageUI 行为或提供新的分享 API；对标产品（Strava Beacon / Uber Share My Trip）改架构 | [live-trip-sharing-and-volunteer-incentives-20260813.md](./live-trip-sharing-and-volunteer-incentives-20260813.md) |
| 2026-08-14 | 内测宣传 / 官网首页用的演示视频怎么做？跟 App Store 里的 App Preview 是不是一回事？ | **不是一回事，规则在关键处相反，必须做两条片子**。App Preview 受审核硬约束：15–30 秒、`886×1920`（iPhone 全现代尺寸统一）、H.264 High L4.0 / 30fps / **必须带立体声 AAC 音轨**、每语言最多 3 条、**默认静音播放**，且**逐字禁止「film people interacting with a device」—— 手指点屏和过肩视角全违规**；而助盲跑最值钱的镜头恰恰是真人跑道画面。官网 hero 版反过来要求**整个删掉音轨**（空音轨会挡自动播放）、≤4MB/桌面、`autoplay muted loop playsinline`、poster 才是 LCP 元素。⇒ 拍一条 75 秒母版，再剪出 ≤30 秒纯录屏版上架。片长由投放位置定不由功能数定（社交 15–30s / 首页 60–90s），前 10–15 秒定生死，Vidyard 数据 1 分钟内完播 65%。**本项目最大技术风险是音频**：已有开发者实证 `AVAudioEngine` voice processing 活跃期间 iOS 内置录屏**整段没有音频**，而我们的语音下单正落在这个面上 —— 必须先拍 15 秒试片回放验证，别等剪辑才发现素材是哑的；模拟器通道不存在（高德无 arm64-sim slice），只能真机录。无障碍：WCAG 1.2.2（A，字幕）+ 1.2.5（AA，音频描述），自动字幕不够用，**一个助盲产品的宣传片自己不可访问是最容易被打脸的地方**。伦理：避开 inspiration porn（自检 —— 把「盲人」设定拿掉还是个故事吗），盲人知情同意走**口头宣读 + 录像**而非签字，成片须本人过目且可撤回。**六条项目红线**（第 1 条最重）：绝不能说「已通知家属 / 联系人已收到短信」（守卫只拦代码，拦不到视频）；SOS 降级分支要说清 App 不代发；不得出现真实手机号与坐标；字幕状态词须与 App 一致；只演示内测真有的功能；没有「现在就跑」（≥30 分钟） | Apple 改 App Preview 规格页或放宽「不得拍摄人操作设备」；WCAG 改 1.2.x 等级要求；`AGENTS.md` §6 的 SOS 文案红线变化或后端短信改为可确认送达；官网技术栈变化导致 hero 体积预算变化 | [demo-video-production-20260814.md](./demo-video-production-20260814.md) |
| 2026-08-14 | 内测怎么发出去？TestFlight 有哪些硬规则？给中国大陆用户做内测要不要先办 ICP / App 备案？ | **内测通道不卡备案，卡的是 Beta App Review + 隐私政策**。TestFlight：内部 100 人（上传即测、免审）/ 外部 10,000 人（**首个构建必过 App Review**，后续可能免全审）/ 构建 **90 天**过期 / 外部测试必填 Beta App Description 与 Beta App Review Information（⇒ 必须给审核员演示账号，用 `docs/test-accounts.md` 的预置号 + 固定码 `000000`）。⚠️ **ICP 那条未拿到一手依据** —— 两个对口的 Apple 官方页（`enter-app-information-for-china-mainland` / `view-mainland-china-compliance-information`）本轮抓取**均 404**，只有搜索转述说「备案针对正式上架而非 TestFlight」，按**未证实**对待，别写进对外承诺。备案确定卡的是**中国区上架**：2023-09-01 起新 App 上架前必须备案，2024-04-01 起无备案号无法更新且强制下架，境外主体同样适用；备案绑**域名**且与服务器所在地绑定，而我们后端是**裸 IP `47.114.113.171` 无域名** ⇒ 上架关键路径是「买域名 → ICP 备案 → 公安备案（30 天内）→ HTTPS → App 改走域名 → App 备案 → 填进 ASC」，每步都有外部等待，**不影响内测但必须现在并行启动**。本轮最有用的产出是**按生效时点重排的门槛表**（§3）：多数人以为「先发内测、上架前补合规」，实际**首个构建就要过 App Review**，于是 5.1.1 整组（应用内隐私政策、应用内账号注销、数据收集披露）**内测第一天就生效**；PIPL 第 29 条的敏感信息单独同意（身份证号、行踪轨迹）同理，给真实用户用就适用。**未查到**：TestFlight App 自身对 VoiceOver 的可用性（无可靠来源，只能拿干净设备闭眼实测「邀请邮件→装 TestFlight→兑换→装 App」全程，这段不在我们代码里、改不了）；「后续构建免全审」的触发边界；视障用户内测的国内伦理规范 | Apple 改 TestFlight 人数/90 天/首建必审规则；**Apple 开始在 TestFlight 校验大陆备案号（这条一发生 §2 结论直接反转）**；工信部改 App 备案适用范围；后端从裸 IP 迁到域名 + HTTPS；App Review 5.1.1 的账号删除/隐私政策要求变化；PIPL 单独同意执行口径变化 | [beta-distribution-and-launch-gates-20260814.md](./beta-distribution-and-launch-gates-20260814.md) |
| 2026-08-19 | 服务提供者端（司机 / 陪同者）「服务进行中」页面的紧急求助入口该放在哪？误触怎么防？ | **四款产品全部是「地图角落固定悬浮图标 + 独立二级页 / 二级确认」，没有一款把紧急按钮混排进常规操作按钮列表** —— Uber Driver 蓝色盾牌在左下角、Lyft Driver 图标在右上角、滴滴「安全中心」在左下角、Lyft+ADT 是独立入口。⚠️「紧急操作应与常规操作空间分离」**查不到逐字的正式规范条款**（Apple HIG / Material / NN/g 都没点名回答），按未证实对待，只有三条间接材料（HIG Action Sheets 的破坏性操作置顶分区、设计系统文献的 >8–16px 间距共识、NN/g User Control and Freedom）。误触防护分四类：多级菜单二次选择（Uber / Lyft）、警示语 + 系统级二次确认（滴滴）、长按 + 10 秒 PIN 解除（Noonlight）、语音口令隐蔽触发（bSafe）。**两处不能照抄**：① 我们志愿者端的 SOS 是代盲人发起，不是志愿者自己的逃生按钮；② 后端对志愿者 `FALSE_ALARM` 恒 403，按错了自己撤不掉，误触代价高于打车场景。已否决 Noonlight 的长按 + PIN（拖慢真实求助，且 PIN 对盲人端不可用）与滴滴的「谎报警情依法追责」警示语（我们不直接报警，写了就是夸大后果） | 对标产品（Uber Driver / Lyft Driver / 滴滴司机端）安全入口改版；后端放开志愿者撤销求助（`EMERGENCY_VOLUNTEER_CANNOT_DISMISS` 取消）；Apple HIG 或 NN/g 新增紧急操作摆位条款 | [volunteer-sos-button-placement-20260819.md](./volunteer-sos-button-placement-20260819.md) |
| 2026-08-14 | SwiftUI 里能不能把 VoiceOver 遍历顺序与视觉叠放顺序解耦（让铺满上半屏的地图排到内容之后）？ | **不能**，`accessibilitySortPriority` 在跨叠放层的场景下是**空操作** —— 真机实测四种排法（裸 sortPriority／换声明顺序 + `zIndex`／三层都补 `children: .contain` 再排／地图改成内容层的 `.background`）地图一律排在内容前面；决定性证据是「换声明顺序 + `zIndex`」时遍历顺序跟着 `zIndex` 走 ⇒ **遍历顺序 = 绘制顺序**。社区资料说「补 `.contain` 就生效」，但例子全是同层若干 Text，覆盖不到重叠叠放层。⚠️ 附带修正：原用例拿深度 1 的地图和深度 3 的按钮比下标（`allElementsBoundByAccessibilityElement` 是**逐层枚举**），27 vs 16 差的是深度不是顺序，**那条断言从来就没有可通过的实现**。采纳方案：装饰性地图改 `accessibilityHidden(true)`（0 次多余划动，优于「排到后面」）。⚠️ **2026-08-22 修正：`accessibilityHidden(true)` 加在外层是无效的** —— `MapViewWrapper` 内部的 `.accessibilityElement(children: .ignore)` + `.accessibilityLabel` 会合成一个新元素，外层盖不住它；当时只在 `disableMap: true`（占位图）路径上验过，而生产构建走的是真 key 那条，洞一直开着（真机实测 `Other 402x200 «地图，显示当前位置和订单地点»` 仍排在内容层前面）。结论本身不变（隐藏仍是对的方案），变的是**隐藏必须发生在元素被合成的那一层**：已改为 `MapViewWrapper.isDecorative`，装饰用法根本不合成该元素。回归钉子 `testRealAMapEnabledSmoke`（已验红）；已否决「地图挪到主操作下方」（推翻 20260808 视觉基线）与「改规格接受划一次」 | Apple 改 SwiftUI 无障碍元素的排序实现或让 `accessibilitySortPriority` 支持跨叠放层；首页不再采用「地图铺满上半屏 + 内容压在上面」的叠层结构 | [swiftui-voiceover-traversal-order-20260814.md](./swiftui-voiceover-traversal-order-20260814.md) |
| 2026-08-21 | 主 App 隐私清单（ITMS-91053）：第三方 SDK 是静态库时算谁的？vendor 的 reason code 能不能照抄？ | **静态库的 required reason API 必须由「主 App 的」清单声明，但不能照抄该 SDK 的 reason code**。阿里云 10 个 framework 全是静态库（`file` 显示 ar archive）⇒ 不会被 embed，它们内嵌的 5 份 `PrivacyInfo.xcprivacy` **一份都进不了产物**（实测产物里 `*.xcprivacy` 只有 1 个），而代码链进主二进制，`statfs`/`attributesOfItemAtPath` 等符号实测在库里 ⇒ 主 App 必须声明 **DiskSpace + FileTimestamp**，哪怕自家 Swift 代码 0 处用到。⚠️ 照抄 vendor roll-up 会把 ITMS-91053 换成 **ITMS-91055**：`3B52.1` 与 `1C8F.1` Apple 逐字写着 "may only be declared by third-party SDKs"，`7D9E.1` 限「健康研究类 App」，`E174.1` 要求磁盘信息显著展示在用户主动提交的 bug 报告里 ⇒ 主 App 只能取 `CA92.1`+`35F9.1`+`85F4.1`+`DDA9.1`。另：阿里云那份的 purpose 写的自由字符串 `Protect Device Security` **不是 Apple 合法取值**（6 个常量里 0 命中），它被当成主 App 清单时本身就非法。**Podfile 全程没动** —— 冲突源是我们自己 vendored 的 podspec 把清单写进了 `s.resources`（平铺到 bundle 根 = 主 App 槽位）。已否决：改 Podfile 加 post_install、挪进 `s.resource_bundles`（阿里云不在 Apple 强制名单里，装了还得带上那条非法取值） | 升级阿里云 SDK（新的 roll-up 清单会变，且 podspec 要重写）；Apple 改 required reason 类别表或 reason code 的可声明主体；本仓库新增用到 required reason API 的代码；阿里云 framework 从静态库改为动态库 | [ios-privacy-manifest-20260821.md](./ios-privacy-manifest-20260821.md) |
| 2026-08-23 | 激励体系（积分/双人火花/邀请码）的**前端呈现层**：火花断裂重开怎么告知、零加分流水怎么解释、邀请码只靠听怎么用、这些界面 VoiceOver 念成什么样 | **邀请码逐字播报有原生 API，不要自己拼顿号串**：`speechSpellsOutCharacters(_:)` 在 `Text` 与 `View` 上都有、`@available(iOS 15.0)`（本机 SDK `.swiftinterface` 实证，部署目标 16 够，不需要 `#available`），且 `accessibilityLabel(_ label: Text)` 是 iOS 14+ ⇒ 可写 `.accessibilityLabel(Text("我的邀请码 ") + Text(code).speechSpellsOutCharacters())`，不必等 iOS 18 的 `AttributedString` 重载；⚠️ 它在 `.combine` 容器里是否保留只有社区教程背书 [中]，**真机必验**（本仓库栽过同型的跟头）。「断了」这个词有官方实证反对：Duolingo 逐字「break your streak... can actually feel quite _de_motivating」⇒ 后端 `PARTNER_STREAK_RESTARTED` 的模板文案照用、**一个字不改，客户端接入成本为零**（未知 eventType 已由 `AppRealtimeCoordinator:724` 通用通道照播）。Strava 反面教材：成就通知**不可关闭**是已知痛点 ⇒ 我们不给它做 TTS 抢播（它到达时用户正在听订单完成播报）。邀请码「只能注册时填、不补」是行业标准（Uber 官方帮助页原文），但 Uber 真实形态是「对外说不能补、对内留 15 天人工补救」，而我们两样都没有、也没有「我的邀请人是谁」端点 ⇒ 填错者永远不会知道，只能靠文案诚实告知（已投 handoff）。⚠️ **纠正一条被广泛转述的错误前提：积分没有开关** —— `PointService.java:44-65` 的 `@Value` 只有数值参数，带 `enabled` 的是火花/优先轮/邀请奖励三个，所以联调时积分会真的动，空态**不能**写「功能未开启」。三个端点的「空」语义各不相同，写成同一个空态就是错的。附朗读脚本 6 屏 | 后端把 `PARTNER_STREAK_RESTARTED` 的 priority 从 NORMAL 改成 HIGH（会补发 APNs，"不做抢播"这条克制就不够了）；后端新增「我的邀请人是谁」字段或补填入口；Apple 改 `speechSpellsOutCharacters` 语义或废弃它；固定搭档上限 `max-per-user=10` 变化或真实用户收藏数普遍 >3（「盲人侧合进一屏」的前提失效）；Strava / Duolingo / Uber 改各自的 streak 或 referral 规则 | [incentive-ui-blind-first-20260823.md](./incentive-ui-blind-first-20260823.md) |
| 2026-08-20 | CoreLocation 自动暂停的确切语义（默认值 / 会不会自动恢复 / in-use 授权下的后果）；ATS 明文例外会不会导致 App Store 拒审 | **默认开且不自动恢复**——Apple 原文「a pause to location updates **ends access to location changes until the app launches again**」（仅 in-use 授权），且必须自己实现 `locationManagerDidPauseLocationUpdates` 重启；官方建议持续追踪类 App 直接关掉该属性。ATS：`NSExceptionAllowsInsecureHTTPLoads` **触发人工审查但不自动拒**，Apple 举的合格理由是「**别人**管的服务器不支持 TLS」，「自家服务器还没配」不在其列；2025–2026 未见针对自有服务器明文的新硬性规则，按未定论处理 | iOS 大版本改 CoreLocation 暂停语义或 Dynamic 授权模型；Apple 更新 *Preventing Insecure Network Connections*；App Review 出台明文相关新规 | [ios-location-pause-and-ats-20260820.md](./ios-location-pause-and-ats-20260820.md) |
