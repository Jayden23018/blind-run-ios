# 调研索引

本仓库**所有**联网调研的唯一落脚点。规则只有三条：

1. **开搜前整份读这个文件**，不要 `ls | grep` —— `ls` 只匹配文件名，搜「地图」命中不了
   `amap-geocoding-20260801.md`；能搜到的词在下面「问题」和「一句话结论」两列里。
2. 已有结论还作不作数，看「复核触发条件」那列。**没触发就直接用，不要重跑。**
   触发了、或表里根本没有这个问题、或有但缺你要的那一段 —— 才开新一轮。
3. 调研完落盘到 `docs/research/{topic}-{YYYYMMDD}.md`，**并回写下面一行**。
   不回写等于没做：下次搜不到，同一份调研会被原样重跑一遍。

被否掉的方案同样留一行 —— 「试过 X 因为 Y 放弃」跟「选了 Z」一样值钱，且更容易被忘。

强制在 `scripts/hooks/research-log.mjs`：联网工具调用前把本文件灌回给模型（第 1 条），
停止前检查本轮联网过但 `docs/research/` 没动（第 3 条）。自测 `scripts/validate-research-log.mjs`。

| 日期 | 问题 | 一句话结论 | 复核触发条件 | 报告 |
|------|------|-----------|-------------|------|
| 2026-08-05 | 视障用户语音预约陪跑的界面与交互标准：决策点数、尾静音时长、确认词白名单、有订单时首页要不要被接管 | 减法方向对但论证支柱错；Magic Tap 不可依赖（iOS 26 起可整个关闭）；确认词白名单必须重做（Portland 事件证据链）；12 秒尾静音超 Nielsen 10 秒上限要改；订单卡应作第 1 个 VoiceOver 焦点而非整页接管；`YD/T 4211-2023` 引错了，它是域名隐私标准 | iOS 大版本改 VoiceOver 手势或 Magic Tap 语义；`Speech` 框架端点检测默认值变化；国标/行标出新版无障碍要求 | [blind-voice-booking-ia-20260805.md](./blind-voice-booking-ia-20260805.md) |
| 2026-08-12 | `dynamicTypeSize(...accessibility3)` 这样的封顶够不够 WCAG 1.4.4 的「放大 200% 不裁切」？ | **够** —— body 在 AX3 是 40pt / 默认 17pt ≈ **235%**，AX5 是 53pt ≈ 312%。此前 review 初稿凭印象写成「AX3 约 175%，够不到 200%」，是错的。封顶仍该去掉，但理由换成「AX4/AX5 正是低视力用户实际会设的档位」+「全仓只有一个组件封顶，同屏缩放不一致比两个极端都差」。附带：不同文本样式缩放速率不同（AX5 档 Body 比 Title 1 还大），不能拿一个样式的比例推另一个 | Apple 改 Dynamic Type 字号表或新增档位；WCAG 改 1.4.4 的倍数要求 | [dynamic-type-scale-20260812.md](./dynamic-type-scale-20260812.md) |
| 2026-08-12 | 市面同类盲人产品各做了哪些功能？一个盲人 App「必须」有哪些能力与适配，强制性从哪来？ | 同类产品分**三类**（即时视觉协助 / 长期配对陪跑 / 无障碍派单出行），功能集不可互抄；AidRun 是跨类产品 —— 用第三类的派单机制承载第二类的人身安全风险面，缺口要按第二类对；可直接照搬的只有滴滴的三节点提醒 + 勋章激励、UIS 的出发前四要素约定 + 一对多储备池（每人需 6–8 名固定陪跑员）、关怀版的预存常用地址；**面向 App 的无障碍国标目前不存在**，实际依据是 GB/T 37668-2019 + 《移动互联网应用（APP）适老化通用设计规范》，信息无障碍标识两年一检 | 工信部《移动互联网应用程序适老化技术规范》正式出台；WCAG 出新版或 EAA 执行口径变化；对标产品（滴滴无障碍出行 / United In Stride / Be My Eyes）发布重大功能改版 | [blind-app-feature-landscape-20260812.md](./blind-app-feature-landscape-20260812.md) |
| 2026-08-12 | 跑完的轨迹回放页怎么做：地图占多大、统计怎么排、我们缺哪块 | 同类产品只有一种版式——地图占顶部 35–45% + 下面卡片装统计，统计是 3 列网格（数字大标签小在下）而非我们现在的左右对齐行；列表内嵌用压缩版、点进去才是大屏。我们不缺渲染能力也不缺后端数据，缺的是：志愿者「近期服务」进的那个详情页压根没挂轨迹组件（「服务记录」进的那个挂了）、盲人端没有历史订单列表、地图写死 220pt 且无起终点标记 | 悦跑圈/Strava 活动详情页重大改版；后端给 `TrackPointDto` 加逐点配速或海拔（配速染色的否决理由随之消失）；再动盲人端首页信息架构 | [run-track-replay-ui-20260812.md](./run-track-replay-ui-20260812.md) |
| 2026-08-13 | 怎么在腾讯会议上同时演示两台 iOS 真机（盲人端 + 志愿者端）？ | 腾讯会议**没有**「手机投屏当共享源」，只能桌面/窗口/白板；正解是 QuickTime「新建影片录制」开**两个窗口**分别镜像两台 USB 设备，再用腾讯会议 3.30+ 的 Shift 多选窗口共享（本机 3.41.1 满足）；AirPlay 到 Mac 只能救一台、不支持双设备并存，仅作退路；⛔ **iPhone 镜像（iPhone Mirroring）出局** —— 镜像期间 iPhone 必须锁定、麦克风不可用，语音下单跑不起来；QuickTime 的 Microphone 要选「无」不要选 iPhone（会劫持音频路由）；⚠️ 排查「QuickTime 看不到设备」时有**两个会给假阴性的检查**：`system_profiler SPUSBDataType` 在本机 returncode=0 但输出 0 字节，据此判「没插 USB」是错的（真实判据用 `ioreg -p IOUSB` 或 `devicectl device info details` 的 `transportType`）；未签名 CLI 枚举 `AVCaptureDevice` 的 muxed 类别也可能拿不全，不能证伪 QuickTime。macOS 26 **未移除**该功能。设备在下拉的 **Camera** 分组（不是 Screen），且**手机必须解锁**、QuickTime 要在插线之后重开；⚠️ **一个 QuickTime 实例只能开一个影片录制窗口**，第二台设备要 `open -n -a` 起第二个实例（Finder「复制 App」的老办法 Ventura 起已失效）；`Finder 能看到 ≠ QuickTime 能看到`；⚠️ QuickTime 镜像有中途断线且**只能重启 Mac** 恢复的已知 bug，直播演示必须备录屏 | 腾讯会议新增移动设备作为共享源；macOS 大版本改 QuickTime 设备捕获行为或修掉镜像中断 bug；演示设备组合变化 | [tencent-meeting-two-ios-device-demo-20260813.md](./tencent-meeting-two-ios-device-demo-20260813.md) |
| 2026-08-08 | 盲人端首页与接单后页面的视觉规格：主按钮多大、圆形还是圆角矩形、次级操作并排还是堆叠 | 全宽圆角矩形，主按钮占内容区约 75%（现状 `minHeight: 64` 差一个数量级）；圆形是错方向（同尺寸只有 π/4 ≈ 78.5% 可点面积）；次级操作一律整行铺满竖直堆叠，对标产品无一并排；接单后那页要从 7 个 section 减到一张头像加一个按钮 | `docs/ui/reference-screenshots/` 下的对标产品发布重大改版；我们改动首页或接单后页面的信息架构 | [blind-ui-visual-benchmark-20260808.md](./blind-ui-visual-benchmark-20260808.md) |
| 2026-08-14 | App 自己的 TTS 播报能不能跟随用户的 VoiceOver 语速？（review §5.6 / §6.1 #18 排进「要产品拍板」的那条） | **不需要拍板，平台已给答案**：读不到也不该读用户语速（无公开 API，且旁白与朗读内容是两个独立滑块），改设 `AVSpeechUtterance.prefersAssistiveTechnologySettings = true`，VoiceOver 在跑时系统用用户的音色与语速覆盖，没跑时才用我们的值 ⇒ `voice`/`rate` 两行必须保留。`API_AVAILABLE(ios(14.0))`，部署目标 16.0 **不需要 `#available`**；⚠️ WebSearch 转述成「iOS 16+」是错的，版本敏感 API 以本机 SDK 头文件为准。出厂值实测为 `false`（缺陷真实存在），但头文件写明查询属性反映不出用户设置 ⇒ **语速在进程里测不出来，单测只能断言开关，听感必须真机人耳验**。附带：`rate` 标度非线性、中点 0.5 不是 1.0。**未解决**：VoiceOver 开着时同一句同时走通告与合成器是否「念两遍」，仍需真机听 | Apple 改 `prefersAssistiveTechnologySettings` 语义或废弃它；本仓库部署目标降到 iOS 14 以下；两条播报通道（通告 + 合成器）的取舍被重新讨论 | [tts-rate-follows-voiceover-20260814.md](./tts-rate-follows-voiceover-20260814.md) |
| 2026-08-13 | 现有联网搜索/抓取配置能不能覆盖竞品调研 / 医学论文 / 政策文件汇报三类场景？该不该加 Exa、Tavily？什么时候派 subagent 抓、什么时候自己抓？ | **不缺工具，缺免费额度和原文通道**。Firecrawl 是 keyless 层且**本轮实测已撞满当日配额**（`free daily limit for this network has been reached`，约 15.9h 恢复）—— 记忆 `firecrawl-mcp-setup` 写的注册触发条件已到。三场景各自的通道已实测钉死：竞品评价走 **HN Algolia API**（免 key，返回原始评论，是识破厂商软文的唯一途径 —— 实测发现 Firecrawl 在买软文）；论文**不要抓网页**，OpenAlex / Europe PMC / PubMed E-utilities 免 key 实测全 200，Semantic Scholar 无 key 已 429；政策文件 **gov.cn 直连即可**（200 / 0.17s），`r.jina.ai` 转 markdown 再省 64%。**Exa / Tavily 暂不上** —— 独立基准（AIMultiple）显示前四名质量差在噪声内，差 20× 的是延迟。Reddit 三条路全堵（`.json` 403 / 内置浏览器策略拦 / firecrawl 限流）。**§9 追加：官方 OAuth 也不是「注册即用」** —— Responsible Builder Policy 要求事先批准，研究用途必须走 RFR 项目、商业用途需书面批准，且把非商业抓取也列入禁止；本仓库的调研用法落在灰色偏违规区，脚本已写好但**暂停使用**，改为 HN Algolia + 人工阅读（这是「端点存在 ≠ 端点可用」的又一例：第一轮只读了限速那节没读准入那节）。subagent 判据改为**按输出物切而非按任务大小切**：要结论就派，要原话/数字/字段就自己抓（原文经过 subagent 已经是转述，进程边界决定，prompt 修不了）。**§8 追加**：`WebSearch` 要定向搜某站必须用 `allowed_domains` **参数**，写进查询串的 `site:` 不生效且会静默退化成 SEO 软文（本报告第一轮据此误判内置搜索没能力）；`reddit.com` 是 Anthropic user agent 层硬屏蔽（400 硬错误）；`s.jina.ai`≠`r.jina.ai`，前者无 key 直接 401，免费 key 解锁的是整个 search 端点；**Exa 不买** —— 其 ToS §4.2(a) 逐字禁止 download/copy/reproduce 任何经服务获得的信息（仅允许浏览器临时缓存），与本仓库「调研必须落盘归档」直接冲突，§1(c) 另授予永久不可撤销许可且 query 用于训练模型（Firecrawl/Perplexity/Linkup/Tavily 条款同样贪，Jina Reader 暴露面不同因为给它的是 URL 不是研究问题） | Firecrawl 或 Jina 改免费层额度；Reddit 改 Data API 政策；gov.cn 加反爬或改站点结构；Exa 改 ToS §4.2；出现有独立第三方基准支撑的新搜索 API | [web-research-toolchain-20260813.md](./web-research-toolchain-20260813.md) |
| 2026-08-13 | 「行程实时分享给紧急联系人」与「志愿者激励体系」，同类产品的 UI 与架构怎么做？iOS 侧有哪些平台约束？ | 实时分享在 Strava Beacon 与 Uber 上是**同一套架构**：服务端发 token + 免登录只读页，客户端只负责把链接塞进**预填短信由用户手动点发送**，有效期绑行程生命周期而非固定 TTL，联系人上限 3–5 人 ⇒ **实时分享必须先有后端契约，纯前端最多做到静态行程告知短信**；激励可抄「按服务量分层（25/50/100/250/500）+ 徽章视觉差 + 进度常驻可见」，要避开排行榜竞争且必须可退出；⚠️ `MFMessageComposeViewController` 的 `.sent` **不保证送达**、预填可被用户改、系统 composer 是 out-of-process 无法注入无障碍标签（播报只能自己在回调里做）。缺口：服务时长**证书**生成没搜到可用材料 | 后端上线分享令牌/只读轨迹契约；Apple 改 MessageUI 行为或提供新的分享 API；对标产品（Strava Beacon / Uber Share My Trip）改架构 | [live-trip-sharing-and-volunteer-incentives-20260813.md](./live-trip-sharing-and-volunteer-incentives-20260813.md) |
| 2026-08-14 | 内测宣传 / 官网首页用的演示视频怎么做？跟 App Store 里的 App Preview 是不是一回事？ | **不是一回事，规则在关键处相反，必须做两条片子**。App Preview 受审核硬约束：15–30 秒、`886×1920`（iPhone 全现代尺寸统一）、H.264 High L4.0 / 30fps / **必须带立体声 AAC 音轨**、每语言最多 3 条、**默认静音播放**，且**逐字禁止「film people interacting with a device」—— 手指点屏和过肩视角全违规**；而助盲跑最值钱的镜头恰恰是真人跑道画面。官网 hero 版反过来要求**整个删掉音轨**（空音轨会挡自动播放）、≤4MB/桌面、`autoplay muted loop playsinline`、poster 才是 LCP 元素。⇒ 拍一条 75 秒母版，再剪出 ≤30 秒纯录屏版上架。片长由投放位置定不由功能数定（社交 15–30s / 首页 60–90s），前 10–15 秒定生死，Vidyard 数据 1 分钟内完播 65%。**本项目最大技术风险是音频**：已有开发者实证 `AVAudioEngine` voice processing 活跃期间 iOS 内置录屏**整段没有音频**，而我们的语音下单正落在这个面上 —— 必须先拍 15 秒试片回放验证，别等剪辑才发现素材是哑的；模拟器通道不存在（高德无 arm64-sim slice），只能真机录。无障碍：WCAG 1.2.2（A，字幕）+ 1.2.5（AA，音频描述），自动字幕不够用，**一个助盲产品的宣传片自己不可访问是最容易被打脸的地方**。伦理：避开 inspiration porn（自检 —— 把「盲人」设定拿掉还是个故事吗），盲人知情同意走**口头宣读 + 录像**而非签字，成片须本人过目且可撤回。**六条项目红线**（第 1 条最重）：绝不能说「已通知家属 / 联系人已收到短信」（守卫只拦代码，拦不到视频）；SOS 降级分支要说清 App 不代发；不得出现真实手机号与坐标；字幕状态词须与 App 一致；只演示内测真有的功能；没有「现在就跑」（≥30 分钟） | Apple 改 App Preview 规格页或放宽「不得拍摄人操作设备」；WCAG 改 1.2.x 等级要求；`AGENTS.md` §6 的 SOS 文案红线变化或后端短信改为可确认送达；官网技术栈变化导致 hero 体积预算变化 | [demo-video-production-20260814.md](./demo-video-production-20260814.md) |
| 2026-08-14 | 内测怎么发出去？TestFlight 有哪些硬规则？给中国大陆用户做内测要不要先办 ICP / App 备案？ | **内测通道不卡备案，卡的是 Beta App Review + 隐私政策**。TestFlight：内部 100 人（上传即测、免审）/ 外部 10,000 人（**首个构建必过 App Review**，后续可能免全审）/ 构建 **90 天**过期 / 外部测试必填 Beta App Description 与 Beta App Review Information（⇒ 必须给审核员演示账号，用 `docs/test-accounts.md` 的预置号 + 固定码 `000000`）。⚠️ **ICP 那条未拿到一手依据** —— 两个对口的 Apple 官方页（`enter-app-information-for-china-mainland` / `view-mainland-china-compliance-information`）本轮抓取**均 404**，只有搜索转述说「备案针对正式上架而非 TestFlight」，按**未证实**对待，别写进对外承诺。备案确定卡的是**中国区上架**：2023-09-01 起新 App 上架前必须备案，2024-04-01 起无备案号无法更新且强制下架，境外主体同样适用；备案绑**域名**且与服务器所在地绑定，而我们后端是**裸 IP `47.114.113.171` 无域名** ⇒ 上架关键路径是「买域名 → ICP 备案 → 公安备案（30 天内）→ HTTPS → App 改走域名 → App 备案 → 填进 ASC」，每步都有外部等待，**不影响内测但必须现在并行启动**。本轮最有用的产出是**按生效时点重排的门槛表**（§3）：多数人以为「先发内测、上架前补合规」，实际**首个构建就要过 App Review**，于是 5.1.1 整组（应用内隐私政策、应用内账号注销、数据收集披露）**内测第一天就生效**；PIPL 第 29 条的敏感信息单独同意（身份证号、行踪轨迹）同理，给真实用户用就适用。**未查到**：TestFlight App 自身对 VoiceOver 的可用性（无可靠来源，只能拿干净设备闭眼实测「邀请邮件→装 TestFlight→兑换→装 App」全程，这段不在我们代码里、改不了）；「后续构建免全审」的触发边界；视障用户内测的国内伦理规范 | Apple 改 TestFlight 人数/90 天/首建必审规则；**Apple 开始在 TestFlight 校验大陆备案号（这条一发生 §2 结论直接反转）**；工信部改 App 备案适用范围；后端从裸 IP 迁到域名 + HTTPS；App Review 5.1.1 的账号删除/隐私政策要求变化；PIPL 单独同意执行口径变化 | [beta-distribution-and-launch-gates-20260814.md](./beta-distribution-and-launch-gates-20260814.md) |
| 2026-08-19 | 服务提供者端（司机 / 陪同者）「服务进行中」页面的紧急求助入口该放在哪？误触怎么防？ | **四款产品全部是「地图角落固定悬浮图标 + 独立二级页 / 二级确认」，没有一款把紧急按钮混排进常规操作按钮列表** —— Uber Driver 蓝色盾牌在左下角、Lyft Driver 图标在右上角、滴滴「安全中心」在左下角、Lyft+ADT 是独立入口。⚠️「紧急操作应与常规操作空间分离」**查不到逐字的正式规范条款**（Apple HIG / Material / NN/g 都没点名回答），按未证实对待，只有三条间接材料（HIG Action Sheets 的破坏性操作置顶分区、设计系统文献的 >8–16px 间距共识、NN/g User Control and Freedom）。误触防护分四类：多级菜单二次选择（Uber / Lyft）、警示语 + 系统级二次确认（滴滴）、长按 + 10 秒 PIN 解除（Noonlight）、语音口令隐蔽触发（bSafe）。**两处不能照抄**：① 我们志愿者端的 SOS 是代盲人发起，不是志愿者自己的逃生按钮；② 后端对志愿者 `FALSE_ALARM` 恒 403，按错了自己撤不掉，误触代价高于打车场景。已否决 Noonlight 的长按 + PIN（拖慢真实求助，且 PIN 对盲人端不可用）与滴滴的「谎报警情依法追责」警示语（我们不直接报警，写了就是夸大后果） | 对标产品（Uber Driver / Lyft Driver / 滴滴司机端）安全入口改版；后端放开志愿者撤销求助（`EMERGENCY_VOLUNTEER_CANNOT_DISMISS` 取消）；Apple HIG 或 NN/g 新增紧急操作摆位条款 | [volunteer-sos-button-placement-20260819.md](./volunteer-sos-button-placement-20260819.md) |
| 2026-08-14 | SwiftUI 里能不能把 VoiceOver 遍历顺序与视觉叠放顺序解耦（让铺满上半屏的地图排到内容之后）？ | **不能**，`accessibilitySortPriority` 在跨叠放层的场景下是**空操作** —— 真机实测四种排法（裸 sortPriority／换声明顺序 + `zIndex`／三层都补 `children: .contain` 再排／地图改成内容层的 `.background`）地图一律排在内容前面；决定性证据是「换声明顺序 + `zIndex`」时遍历顺序跟着 `zIndex` 走 ⇒ **遍历顺序 = 绘制顺序**。社区资料说「补 `.contain` 就生效」，但例子全是同层若干 Text，覆盖不到重叠叠放层。⚠️ 附带修正：原用例拿深度 1 的地图和深度 3 的按钮比下标（`allElementsBoundByAccessibilityElement` 是**逐层枚举**），27 vs 16 差的是深度不是顺序，**那条断言从来就没有可通过的实现**。采纳方案：装饰性地图改 `accessibilityHidden(true)`（0 次多余划动，优于「排到后面」）。⚠️ **2026-08-22 修正：`accessibilityHidden(true)` 加在外层是无效的** —— `MapViewWrapper` 内部的 `.accessibilityElement(children: .ignore)` + `.accessibilityLabel` 会合成一个新元素，外层盖不住它；当时只在 `disableMap: true`（占位图）路径上验过，而生产构建走的是真 key 那条，洞一直开着（真机实测 `Other 402x200 «地图，显示当前位置和订单地点»` 仍排在内容层前面）。结论本身不变（隐藏仍是对的方案），变的是**隐藏必须发生在元素被合成的那一层**：已改为 `MapViewWrapper.isDecorative`，装饰用法根本不合成该元素。回归钉子 `testRealAMapEnabledSmoke`（已验红）；已否决「地图挪到主操作下方」（推翻 20260808 视觉基线）与「改规格接受划一次」 | Apple 改 SwiftUI 无障碍元素的排序实现或让 `accessibilitySortPriority` 支持跨叠放层；首页不再采用「地图铺满上半屏 + 内容压在上面」的叠层结构 | [swiftui-voiceover-traversal-order-20260814.md](./swiftui-voiceover-traversal-order-20260814.md) |
| 2026-08-21 | 主 App 隐私清单（ITMS-91053）：第三方 SDK 是静态库时算谁的？vendor 的 reason code 能不能照抄？ | **静态库的 required reason API 必须由「主 App 的」清单声明，但不能照抄该 SDK 的 reason code**。阿里云 10 个 framework 全是静态库（`file` 显示 ar archive）⇒ 不会被 embed，它们内嵌的 5 份 `PrivacyInfo.xcprivacy` **一份都进不了产物**（实测产物里 `*.xcprivacy` 只有 1 个），而代码链进主二进制，`statfs`/`attributesOfItemAtPath` 等符号实测在库里 ⇒ 主 App 必须声明 **DiskSpace + FileTimestamp**，哪怕自家 Swift 代码 0 处用到。⚠️ 照抄 vendor roll-up 会把 ITMS-91053 换成 **ITMS-91055**：`3B52.1` 与 `1C8F.1` Apple 逐字写着 "may only be declared by third-party SDKs"，`7D9E.1` 限「健康研究类 App」，`E174.1` 要求磁盘信息显著展示在用户主动提交的 bug 报告里 ⇒ 主 App 只能取 `CA92.1`+`35F9.1`+`85F4.1`+`DDA9.1`。另：阿里云那份的 purpose 写的自由字符串 `Protect Device Security` **不是 Apple 合法取值**（6 个常量里 0 命中），它被当成主 App 清单时本身就非法。**Podfile 全程没动** —— 冲突源是我们自己 vendored 的 podspec 把清单写进了 `s.resources`（平铺到 bundle 根 = 主 App 槽位）。已否决：改 Podfile 加 post_install、挪进 `s.resource_bundles`（阿里云不在 Apple 强制名单里，装了还得带上那条非法取值） | 升级阿里云 SDK（新的 roll-up 清单会变，且 podspec 要重写）；Apple 改 required reason 类别表或 reason code 的可声明主体；本仓库新增用到 required reason API 的代码；阿里云 framework 从静态库改为动态库 | [ios-privacy-manifest-20260821.md](./ios-privacy-manifest-20260821.md) |
| 2026-08-23 | 激励体系（积分/双人火花/邀请码）的**前端呈现层**：火花断裂重开怎么告知、零加分流水怎么解释、邀请码只靠听怎么用、这些界面 VoiceOver 念成什么样 | **邀请码逐字播报有原生 API，不要自己拼顿号串**：`speechSpellsOutCharacters(_:)` 在 `Text` 与 `View` 上都有、`@available(iOS 15.0)`（本机 SDK `.swiftinterface` 实证，部署目标 16 够，不需要 `#available`），且 `accessibilityLabel(_ label: Text)` 是 iOS 14+ ⇒ 可写 `.accessibilityLabel(Text("我的邀请码 ") + Text(code).speechSpellsOutCharacters())`，不必等 iOS 18 的 `AttributedString` 重载；⚠️ 它在 `.combine` 容器里是否保留只有社区教程背书 [中]，**真机必验**（本仓库栽过同型的跟头）。「断了」这个词有官方实证反对：Duolingo 逐字「break your streak... can actually feel quite _de_motivating」⇒ 后端 `PARTNER_STREAK_RESTARTED` 的模板文案照用、**一个字不改，客户端接入成本为零**（未知 eventType 已由 `AppRealtimeCoordinator:724` 通用通道照播）。Strava 反面教材：成就通知**不可关闭**是已知痛点 ⇒ 我们不给它做 TTS 抢播（它到达时用户正在听订单完成播报）。邀请码「只能注册时填、不补」是行业标准（Uber 官方帮助页原文），但 Uber 真实形态是「对外说不能补、对内留 15 天人工补救」，而我们两样都没有、也没有「我的邀请人是谁」端点 ⇒ 填错者永远不会知道，只能靠文案诚实告知（已投 handoff）。⚠️ **纠正一条被广泛转述的错误前提：积分没有开关** —— `PointService.java:44-65` 的 `@Value` 只有数值参数，带 `enabled` 的是火花/优先轮/邀请奖励三个，所以联调时积分会真的动，空态**不能**写「功能未开启」。三个端点的「空」语义各不相同，写成同一个空态就是错的。附朗读脚本 6 屏 | 后端把 `PARTNER_STREAK_RESTARTED` 的 priority 从 NORMAL 改成 HIGH（会补发 APNs，"不做抢播"这条克制就不够了）；后端新增「我的邀请人是谁」字段或补填入口；Apple 改 `speechSpellsOutCharacters` 语义或废弃它；固定搭档上限 `max-per-user=10` 变化或真实用户收藏数普遍 >3（「盲人侧合进一屏」的前提失效）；Strava / Duolingo / Uber 改各自的 streak 或 referral 规则 | [incentive-ui-blind-first-20260823.md](./incentive-ui-blind-first-20260823.md) |
| 2026-09-06 | 服务提供方端「已接的远期预约单」怎么设计：入口放哪、临期确认怎么做（跨天预约 `SCHEDULED_CONFIRMED` 上线） | **打车业两家根本没有「确认」这个动作，这条抄不了** —— Uber Reserve / Lyft Scheduled pickups 的闸门是「pickup 前 30/45 分钟必须在线」，靠的是司机有「上线抢单」这一持续状态；我们的无偿志愿者没有等价物（`isAvailable` 是「愿不愿收派单」不是「此刻在岗」），拿它当闸门等于让关掉开关的人自动丢掉已答应的预约。对口先例在**志愿者排班软件**：`confirm-or-release`（一次点击，要么确认要么释放名额），且**确认与释放并置** —— 易取消是特性不是漏洞，把释放做难只会把 no-show 从「提前告知」变成「当天失联」。**入口一律独立于「当前进行中」那个位**，Uber(Opportunities Center) / Lyft(Scheduled pickups tab) / Rover(Upcoming inbox) 三家无一例外；对我们尤其硬，因为 `activeVolunteerOrder` 按 `createdAt` 降序取第一个（`VolunteerHomeView.swift:113-118`），共用一个 slot 时新接的即时单必然顶掉跨天单，而 60 分钟闸门不会因为他在忙就暂停。Rover 是**反面教材**（改期藏在三点菜单、接受发生在会话线程，被点名 "a notable context switch"）⇒ 确认按钮直接摆在预约卡上，不进溢出菜单不进二级页。**已否决**：抄 Uber/Lyft 的在线状态闸门、客户端算 T-120 分钟才显示按钮（120 是后端配置 `departure-confirm-window-minutes`，写死就是第二个源）、收到通知才显示（`WSAppNotification` 不带 `orderId` 且 App 被杀即丢）、全屏拦截 sheet。⚠️ 「提前 24–48 小时提醒降低 no-show 30–40%」**不可引用**（vendor-published 且 uncited，搜索结果自己标了 treat cautiously）。⚠️ 本轮全部经 WebSearch 转述、**未抓原文**；中文同类产品（滴滴司机端预约单）与 Wag 遛狗员侧**没查到** | 对标产品（Uber Driver Reserve / Lyft Scheduled pickups / Rover）改预约单入口或加入临期确认动作；后端把 `departure-confirm-window-minutes` 或 `departure-gate-lead-minutes` 的语义改成客户端可见；志愿者端首页信息架构重排；本仓库真的拿到自己的 no-show 基线数据 | [volunteer-scheduled-order-confirm-ui-20260906.md](./volunteer-scheduled-order-confirm-ui-20260906.md) |
| 2026-09-02 | 有没有适配本项目的 Claude Code skill / 使用方式？尤其界面美观设计与盲人适配 | **最热门的两类现成方案适用度是零**：a11y skill（masuP9 / airowe / alirezarezvani / jeremylongshore）全部建立在 axe-core / jsx-a11y / Playwright 上，检查对象是 ARIA role、tab order、`alt`，在 iOS 上没有对应物；design-review（OneRedOak）同样依赖 Playwright MCP。**Axiom 是这个领域最完整的项目（273 skills + 42 agents）但要否掉** —— 它最对口的 `xcui`/`xclog` 两个工具驱动的是**模拟器**（本仓库永久不可用），且要求 iOS 26+ SDK 而部署目标是 16。**唯一推荐装的是 `swiftui-pro`（twostraws，MIT，刻意写短省 token，显式覆盖 VoiceOver，切入点是「LLM 实际会犯的错」）**。awesome-claude-design 的 DESIGN.md 那套**对原生 mobile 指导为零**（已逐条核实），但它收录的社区批评可用：AI 出的界面有「指纹」（同款 teal 强调色 / 会闪的状态点 / 三列网格 / 每卡左侧竖线），反制要**逐条点名拒绝**而不是说「好看一点」。**本轮真正的产出是两个自家缺口**：🔴 **`.textClipped` 与 `.trait` 从未启用** —— 全仓 `performAccessibilityAudit` 只有 1 处调用（`AccessibilityAuditTests.swift:683`），类型集合是 `[.contrast, .dynamicType, .elementDetection, .hitRegion, .sufficientElementDescription]`，而同文件 `:560-566` 注释逐字宣称「审计里真正抓这件事的是 `.textClipped` 与 `.hitRegion`」并把三条横屏用例定为「唯一能验横屏裁切的通道」⇒ **横屏裁切从来没被检查过，三条用例绿的是另外五项**，与 08-22 真机 AX5 实测的「指标格截断成…」是同一缺陷面（修法一行，但预期会变红，本轮设备未连接**没跑过**）；~~🟡 **9 处 `attachScreenshot` 拍的图从未被导出**~~ —— **2026-09-15 复核：已修，这条作废。**`device-test.sh:224-242` 现在会自动 `xcrun xcresulttool export attachments` 导出到 `<bundle 目录>/attachments`，并打印个数与 `manifest.json` 路径（附件归属比文件名可靠），导出失败不改变测试结论。实际调用点也已从 9 处增至 **11 处**（脚本注释里的「9 处」同样已过期）。⚠️ 仍然成立的限制：UI 测试构建永远没有高德 key，地图是占位图；且**盲人端陪跑进行中那屏目前没有任何截图调用点**（现有 11 处集中在志愿者端 + 盲人首页 + 登录）。API 事实以本机 SDK 头文件为准：`API_AVAILABLE(macos(14.0), ios(17.0))`，iOS 侧 7 个审计类型 | 模拟器通道恢复（高德出 arm64-sim slice）—— 会同时翻转 Axiom 与截图链两条结论；出现原生 iOS/XCUITest 而非浏览器的 a11y skill；本仓库部署目标提到 iOS 26；Apple 改 `XCUIAccessibilityAuditType` 类型表 | [claude-code-setup-for-ios-a11y-20260902.md](./claude-code-setup-for-ios-a11y-20260902.md) |
| 2026-09-07 | 首启「使用帮助」页进入时该不该自动全文朗读？「第一/第二/第三」该不该列？ | **苹果没有「进屏幕自动朗读全文」这个概念**，也没有对应 API 或要求 —— 官方口径是「光标移进新内容区」再由用户自己浏览，标准里反复出现的是 "timely but **non-disruptive**"（一手抓取 App Store Connect《VoiceOver evaluation criteria》）⇒ 「不自动全念」在 VoiceOver 通道下**不是缺陷**。但本项目有第二条通道（不开读屏的用户靠自家合成器），判据应改成「**VoiceOver 在跑就别抢，没开才自己念**」。唯一够得上硬条款的是 **WCAG 2.2 SC 1.4.2 Audio Control（Level A）**：自动播 >3 秒必须给停止手段，且系统音量键不算 —— 现状自动播 40–60 秒、页面上零停止入口、离开也不停，**不合规**。「第一/第二/第三」**保留**：苹果那句 concise 管的是**控件标签**不是正文，引到正文上是错引，而线性播报里序号是唯一的进度线索。底部常驻栏对 SC 2.4.11（AA）**是过的**（`safeAreaInset` 自动留 inset），叠影属另一条线。⚠️ 用户报的「只念一句就停」**不是政策问题也不是 `.announcement` 被导航打断**，是本仓库自己的竞态：首页 `loadActiveOrder` 回来后 `speakCurrentStatus()` 触发 `SpeechService.speak`，而它第一件事是 `stopSpeaking(.immediate)`，把引导页念到一半的说明切断；两句都以「欢迎…助盲跑」开头所以听着像只念了标题 | Apple 改 VoiceOver evaluation criteria 或 Accessibility Nutrition Labels 判定口径；WCAG 改 1.4.2 / 2.4.11；本仓库改掉「引导页先 push、首页播报后到」这个启动顺序；两条播报通道（通告 + 合成器）的取舍被重新讨论 | [help-screen-autoread-and-policy-20260907.md](./help-screen-autoread-and-policy-20260907.md) |
| 2026-08-20 | CoreLocation 自动暂停的确切语义（默认值 / 会不会自动恢复 / in-use 授权下的后果）；ATS 明文例外会不会导致 App Store 拒审 | **默认开且不自动恢复**——Apple 原文「a pause to location updates **ends access to location changes until the app launches again**」（仅 in-use 授权），且必须自己实现 `locationManagerDidPauseLocationUpdates` 重启；官方建议持续追踪类 App 直接关掉该属性。ATS：`NSExceptionAllowsInsecureHTTPLoads` **触发人工审查但不自动拒**，Apple 举的合格理由是「**别人**管的服务器不支持 TLS」，「自家服务器还没配」不在其列；2025–2026 未见针对自有服务器明文的新硬性规则，按未定论处理 | iOS 大版本改 CoreLocation 暂停语义或 Dynamic 授权模型；Apple 更新 *Preventing Insecure Network Connections*；App Review 出台明文相关新规 | [ios-location-pause-and-ats-20260820.md](./ios-location-pause-and-ats-20260820.md) |
| 2026-09-08 | 「不同意人脸认证」的替代路径 iOS 侧怎么做：入口放哪一层、拒绝后那屏说什么、要不要劝用户回去做人脸 | 入口必须与「开始活体认证」**同屏同级、整行铺满**（监管点名反面做法是「灰色小字 / 藏在客服二级菜单」），与本仓库 `PrivacyConsent.declineButtonTitle` 既有口径同向；拒绝后**不许劝返**（GB/T 41819-2022 举例「48 小时内提示 >1 次」即为反面），回头入口做成用户主动可达的静态入口而非提示；时延文案**不得编 SLA**，逐字沿用法律文本「审核时间较长，但结果等效」——解读文章建议的「1 个工作日内反馈」照抄即与对外文本不一致。附：我们走的「二要素 + 人工审核」在替代方式分级里属偏强组合，后端硬性要求 `idVerifyStatus == APPROVED` 与该分级逻辑一致，不是 bug | 《人脸识别技术应用安全管理办法》修订或网信办出配套细则；GB/T 41819 改「频繁提示」的量化举例；`demo/docs/legal/` 改写替代路径描述或加具体 SLA；后端给 decline 路径加审核时长字段 | [face-verify-decline-alternative-path-ux-20260908.md](./face-verify-decline-alternative-path-ux-20260908.md) |
| 2026-09-10 | 视力状况（全盲/低视力）与导盲犬这类残障身份信息的采集表单怎么做：同类产品分几档、必填还是可跳过、PIPL 单独同意在移动端什么形态、后端两档够不够 | **两档够用，不用向后端提枚举扩展** —— United In Stride 注册表原文（Wayback 存档，本轮自己抓的 Gravity Forms 字段）只有两个选项 `Visually Impaired (B2/B3 – Some vision)` / `Totally Blind (B1 – No vision)`，而它**自己的赛事页列的是 B1/B2/B3 三档** ⇒ 档数跟**用途**走不跟标准走；RNIB 官方 CVI 同样两档、Be My Eyes 干脆不问。中国 GB/T 26341-2010 是 4 级、ICD-11 六档、IBSA 三档，但**没有任何撮合类产品把医学分级原样接进用户资料**（那些分级要临床测量，用户自评给不出）。问法用**功能性自述**（UIS 的 "How would you characterize your vision?"、Washington Group 官方 FAQ 明确反对是非题），选项写「完全看不见 / 还有一些视力」，不写残疾等级。🔴 **真正改变设计范围的一条：该收的不止 `visionLevel`** —— RNIB 官方引导指南逐字「there are no hard and fast rules…ask them how they like to be guided」，分级到引导方式没有稳定映射 ⇒ 已有的 `TetherPreference`（`ProfileModels.swift:419`，同样零采集入口）才是志愿者真正需要的，而**它不敏感** ⇒ 拆到单独同意门之外，拒绝视力状况的用户仍能告诉志愿者怎么引导他，「可拒绝」才在产品上成立。同意形态：全屏页与弹窗都合规，**唯独「隐私政策整体勾选框」不构成单独同意**（GB/T 42574-2023），默认勾选是 2019 四部门《认定方法》与 2026-06-03 网信办通报 71 款 App 的高频违规项 ⇒ 既有 `ConsentDisclosureView` 两按钮形态保持。🚩 **一条推理更正**：PIPL 第十六条（不得因拒绝而拒绝服务，**必需的除外**）与第二十九条（单独同意）是**独立两层义务**，「拒绝=不能用 ⇒ 捆绑同意违规」不成立；分层的真正依据换成 GB/T 42574 的一次性同意豁免要求「拆开就达不成目的」，而我们恰恰能拆 ⇒ **不能**打包。**争议未决**：把敏感信息下发给候选（含未接单）志愿者是否构成 PIPL 第二十三条的「向第三方提供」—— 学界分歧、无权威裁判、已投 handoff。⚠️ 第 4 节国标条款**全部来自解读文章转述**（openstd 不提供免费全文），引进对外法律文件前须购正式文本核实 | 后端给 `visionLevel` 加枚举值或上线 `NOT_SPECIFIED`；后端把 `visionLevel` 接进派单打分（两处「不参与排序」文案与用例同时作废）；UIS 迁到 Achilles Rosterfy 后注册表改版；GB/T 42574-2023 或 35273-2020 出新版或拿到官方全文；网信办就「助残类 App 敏感信息下发给其他用户」发通报；产品接入残联补贴/赛事报名/保险核保（届时两档作废，要加的是可上传残疾人证的字段而非枚举值） | [vision-level-collection-ui-20260910.md](./vision-level-collection-ui-20260910.md) |
| 2026-09-09 | 志愿者线上培训该做成什么样：有没有强制性依据？图文还是视频？要不要考试、及格线定多少？ | **政策依据是硬的**——中央社会工作部《关于志愿者招募和培训的工作指引（试行）》（2025-06-05）原文「对需要专门知识、技能的志愿服务坚持**先培训再上岗**」「志愿者在**培训合格后**参与志愿服务活动」，且明确认可**线上笔试 + 情景模拟**两种考核形式、允许**颁发培训证书** ⇒「未完成必修培训不能接单」不是我们自己加的门槛。🚩 该文件还要求记录**学习时长**，这条直接决定数据模型（旧设计的 `training_progress` 里没有这个字段，不查这份文件不会想到）。形态取「图文 Markdown + 情景选择题」：**视频不做** —— Achilles International 确实用 guide training video，但它是**审核通过后邮件单向发送、不承担考核职责**，与我们要产出「合格/不合格」的用途不同，拿它论证 App 内视频课是错引。**及格线的业界锚点是 85 分不是全对**（人民政协网 2023-12-04 报道国内跑团「以 85 分以上的高分通过笔试，考题全部是陪跑中可能出现的状况该如何正确处理」，逐句 fetch 原文核对过），本项目拍板取更严的**全对**，属有意选择。同篇报道给了可直接做成第一道题的真实陷阱题（人流对冲的无障碍设施该停下等人流走掉）与牵引绳指令原话（「往左我就拉一下，往右，我就顶他手臂一下」）。**被否掉的对标：网约车司机培训不可比** —— 司机的真实门槛是政府发的从业资格证（行政许可，85 题/80 分合格），而平台自己的 App 内培训基本是摆设（2016 澎湃：注册专车只需传证件照即可接单，全程不需要线上答题）；「学时打卡」概念其实来自驾照培训大纲，照它学会引向「刷学时」而非「考会了没有」。⚠️ 全国统一的《陪跑员培训考核办法》**不存在**，搜不到不是没搜到 | 中央社会工作部换发新版指引（现行是**试行**版）；出台助残/陪跑服务的专门培训或上岗标准（国标/行标/残联规程）；及格线要从「全对」放宽时直接用 85 分锚点不必重查；Achilles 把 guide training 改成带考核的形式 | [volunteer-training-model-20260909.md](./volunteer-training-model-20260909.md) |
| 2026-09-14 | 服务提供方主屏（地图 + 底部面板 + 卡片流）的「激励层」放在哪个位置、什么视觉权重？有哪些被点名的反面做法？后续改界面去哪找参考图？ | **结构原型是滴滴车主端 5.0**（与我们同形、同问题）：全部信息按重要度压进**模态 / 非模态 / 普通信息**三种卡片，**激励属第三档**，且「成就感」被定位为**事件节点**而非常驻大区块；⚠️ 它公布的具体数值只有「字号基于 4 的整数倍」，圆角/间距/布局坐标一概没有 ⇒ **抄结构不抄像素**。Uber Driver 则把单个最强激励数字（实测 `$8.31` / `$100.77`）提到**顶部常驻 chrome**，激励卡片流另放在 Earnings Tracker（官方工程博客：Status/Browse/Bulletin 三 mode，卡片含 quest progress、loyalty progress）。🔴 **三条必须反着做**：①**不得在用户下线/退出时用目标挽留** —— Uber 那套被 NYT 点名，印度 CCPA 定义下属 dark pattern（「Moving Targets」），而 Motivation Crowding Theory（Frey & Jegen 2001）给出判据：外在激励被感知为 **controlling** 时挤出内在动机、**supportive** 时才叠加，**无偿志愿者是纯内在动机人群，风险高于网约车司机**（网约车侧 Hua 2020 实测是 crowd-in，志愿/知识分享侧 Osterloh 2007 是 crowd-out）⇒ 激励层只陈述不做闸门，无倒计时、无会变的门槛、不写「还差 1 单就…」；②**不得把服务时长折算成金额**（业界通行做法是按 Independent Sector ~$33/小时折算，撞网信办 2026-06-19 通知第 2 条）；③**一屏只强调一个主指标**，同等强调多个等于没强调。参考源：真实上架产品走 Mobbin / Page Flows / Refero / Pttrns / UXArchive（可跨版本对比）/ AppShots，Dribbble·Behance·Collect UI **只看视觉方向不做 UX 决策**（底料是稿不是上架产品），中文设计复盘走优设网 / 站酷；⚠️ **`screenlane.com` 已停运**，现重定向到 Page Flows。附：`claude-code-setup-for-ios-a11y-20260902` 记的 AI 界面指纹「三列网格」与 `run-track-replay-ui-20260812`「跑步统计就该用三列网格」不冲突 —— 问题是不加区分地到处用 | 滴滴车主端或 Uber Driver 首页版式重大改版；后端打开 `app.incentive.leaderboard.enabled`（届时要补「榜单怎么摆」这一段，本轮刻意没查）；本仓库志愿者端首页改掉「地图铺满 + 底部面板」这个叠层结构；Mobbin / Page Flows 等改免费层或停运 | [volunteer-home-incentive-layer-20260914.md](./volunteer-home-incentive-layer-20260914.md) |
| 2026-09-14 | 志愿者首屏改成「个人身份 / 影响力」优先时，Strava 的 profile / 统计 / 奖杯柜 / 活动流怎么组织？志愿者类产品的影响力面板长什么样？底部「滑动开始」有没有权威规格？ | Strava 可抄的是**结构不是外观**：紧凑身份行 → 一个 3× 于其他数字的主指标 → 3 列同质统计网格 → 一排徽章 +「全部 ›」→ 紧凑活动流，且**主操作不在个人页上**（Record 是独立 tab）；中文跑步产品（悦跑圈）统计网格是**数值在上标签在下**，与 Strava 相反，中文语境按中文来。志愿者类产品**没有可抄的影响力面板** —— HelpUnity 的 Profile 是反面教材（影响力用 13px 蓝字，视觉权重低于「Account Settings」行），Rosterfy 把 "track their impact" 写进官方文案却没公开该界面，Lasagna Love 公开图全是 onboarding，只能取暖色人本语气。滑动 CTA 有权威规格（**Uber Base Sliding button**）：阈值 Low=**20%** / High=**80%**，官方 Caution 逐字提醒「动作不关键时滑动只是徒增复杂度」⇒ 滑动必须绑在**真有后果**的动作上；且必须给辅助技术留标准 action，不能只有裸拖拽。**三条查到但不能用**：①累计里程 —— 后端 spec 逐字写「**刻意没有**（跨订单求和会把 OOM 风险搬进成就页）」⇒「86km 陪伴距离」做不出来也别提需求；②「本月 8/10 次」与同比涨跌 —— 撞上一轮已立的 Moving Target 红线，替代物是**外部固定门槛**国标星级（`VolunteerStarLevelDto` 已有）；③时长折算金额 —— 网信办 2026-06-19 通知 + 民政部令第 67 号，且后端明令**不得用「证明/证书」措辞**。⚠️ 参考源订正：`uxarchive.com` 已挂（Cloudflare Error 1000），`apps.apple.com` 被浏览器策略拒 ⇒ 改用 Google Play 商店页取真实截图（`=w526-h296-rw` 换成 `=w360` 取完整竖图，`=w900` 取不到） | Strava 改 You tab 版式或奖杯柜；Uber Base 改 Sliding button 阈值或用途判据；后端给成就页补累计里程或改 `VolunteerStarLevelDto` 门槛口径；`uxarchive.com` 恢复或 Google Play 改截图 CDN 规则；志愿者端首页重新改回「地图铺满 + 底部面板」 | [volunteer-profile-first-screen-20260914.md](./volunteer-profile-first-screen-20260914.md) |
| 2026-09-11 | 🔚 **2026-09-23 已被「星火同行」替代**（见本表 09-23 那行；PR #174 关闭，分支留作存档）—— 「星光地图」：可缩放中国地图上按注册用户点亮发光点（志愿者/盲人分色、夜间星座质感、新注册点亮动画），技术路线 / 合规 / 隐私 / 效果参考 | **技术可做，但卡在一道行政门槛和一条隐私红线上**。🔴 合规：App 内可缩放中国地图属「互联网地图服务」，**必须送审并在地图左下角标注审图号**（有效期 2 年、每 6 个月备案新增标注），免审例外（「内容简单」「不涉及行政区域界线」）不覆盖这个形态；**用高德底图不转移责任** —— 高德合规指南逐字写「开发者应落实安全主体责任……标示审图号」「若计划对所调用的地图内容进行修改的，请注意依法履行地图审核程序」，且保留收回服务权限的处置权；自绘 GeoJSON 是更危险的路（国界须按标准样图，2018 年 8 起「问题地图」通报全是自制地图，开源中国 GeoJSON 普遍 10 年未更新且无 license）。🚩 **本仓库现状：全仓 0 处提及审图号，而我们已在展示高德地图 —— 这是与本功能无关、独立成立的既有缺口**。🔴 隐私：Strava 2018 泄露美军基地的机制是「聚合在低基数区域退化成逐人定位」，我们**全国都处在退化区**（内测几十人），且被点亮的是视障人士（可反推的敏感健康信息）⇒ **盲人的点不得以个体粒度上图**，改成城市级计数 + k-匿名下限（服务端执行，不能交客户端），这同时更贴合产品意图（盲人要的是「有人愿意帮我」不是「还有别的盲人」）。技术：**MapKit 出局**（`MKStandardMapConfiguration.EmphasisStyle` 只有 `.default`/`.muted`，做不出星空）；唯一合规+有官方性能背书的是高德 `MAMultiPointOverlay`（5.1.0+，官方称支持至 10 万点，我们 11.1.200 已满足），但有**两个硬约束**：`MAMultiPointItem 属性不支持动态更新`（点亮动画在这层做不了）、`纹理不受 alpha 影响`⇒一个 overlay 内所有点共用一张 icon（无法逐点发光差异）—— 修法是分层：海量点画静态星空 + 单个 `MAPointAnnotation` 承载点亮动画；高德**没有**内置 cluster 类（`MAAnnotationCluster` 是 Demo 示例代码不是 SDK 类），Swift 生态只有 `efremidze/Cluster`（1295★，2025-07 活跃）在维护，无 Supercluster 的 Swift 移植；全国→街道跨数量级必须**服务端按 zoom 预聚合**，而后端 101 个端点里**零**分布/统计类接口 ⇒ 必须先有契约。产品前提：自建密度模拟实测，**40 人（内测真实规模）时效果是反的**（传达「只有这么几个人」），GitHub Globe 用 12,000 点、deck.gl 英国图用 140,100 点才画出轮廓 ⇒ 万级以下不要上全国空图。无障碍：**对全盲用户价值为零**（装饰地图按既有结论要 `accessibilityHidden`），听觉版「你所在城市有 37 位志愿者」成本更低、价值更高、**零合规敞口，应当先于视觉版上线**。视觉参照是 GitHub Globe 不是 Strava（后者是轨迹密度不是逐人点）；助盲跑同类（United In Stride / GoodSAM / Be My Eyes）全是纯功能地图，情绪化表达在本领域是空白。**两个未定论必须人工确认**：①在合规底图上叠加自有点位算不算「修改地图内容」②仅调色是否触发送审 —— 均无官方定性，拿到答复前不写代码 | 自然资源部就「App 叠加自有点位/仅调色」出台明确口径；《地图管理条例》或《地图审核管理规定》修订；高德改开发者合规指南的责任划分或推出带审图号的合规叠加方案；后端上线用户分布聚合端点；高德 iOS SDK 让 `MAMultiPointItem` 支持动态更新或放开逐点 alpha；产品把需求改为「只到城市级、不下钻街道」（免审例外可能成立，方案整体降一个数量级）或改为「只对内演示不进 App」（全部合规义务不适用）；真实注册量进入万级 | [community-starlight-map-20260911.md](./community-starlight-map-20260911.md) |
| 2026-09-11 | 🔚 **2026-09-23 已被「星火同行」替代**（见本表 09-23 那行；PR #174 关闭，分支留作存档）—— 「一束光地图」**视觉方向**（第二部分，与上一行互补：上一行管「能不能做」，本行管「做成什么样」）：3 个视觉方向 × 12 维度、视觉参考板、辉光与聚合的具体做法 | **推荐方向 C「灯火」，用方向 B 的合规路径兜底**。🔑 **本轮最重要的发现：夜光影像底图把上一行那条「40 人规模下效果是反的」解决掉了** —— 中国轮廓由真实城市灯火构成，与我们有多少用户无关，用户的光叠在灯火之上更亮更冷，40 人时画面依然满、且那 40 束光因对比反而更突出。素材已实测可得可商用：NASA Black Marble 官方下载页给三档分辨率（最高 `86400×43200` 500m 分块，中国主要在 `D1` 块），`BlackMarble_2016_3km.jpg`（13500×6750）浏览器内 3.9 秒加载完，条款逐字「generally are not subject to copyright in the United States」+ 商用需「must not…convey NASA's endorsement」+「NASA should be acknowledged as the source」。🚩 **一条待确认、可能改变合规路径的观察**：实测裁出的中国区域夜光影像上**没有任何国界线/行政界线/地名**，它是卫星影像不是「编制」的地图，**可能不触发地图审核** —— 但叠加自有点位后是否构成互联网地图服务无官方定性，**不要当成已绕开审图号**，与上一行 §1.7 两条一并咨询。三方向取舍：**A 星图**情绪强但轮廓靠「基底星」硬撑＝画面上的点不对应任何人，在信任型助残产品里是错误选择，且自绘轮廓合规风险最高；**B 静夜**（高德幻影黑压暗）成本最低合规最清晰但最不特别；**C 灯火**贴合「万家灯火」中文意象，且**灯火是地面的、人间的**，比天上的星更贴「身边有人」。从 A 里**单独移植一件东西**：放大后在**真实陪跑过**的志愿者与视障跑者之间画极淡连线 —— 星座的本义就是「连起孤立的星才成图形」，正是配对语义；但**连线只对当事人可见**，否则是社交图谱泄露。质感结论（Stellarium 实测）：**精致来自克制** —— 星点小而锐利、**没有强辉光**、底色是极微妙渐变而非纯黑，辉光越多越廉价；辉光照 Mapsmith 的**双层结构**（宽暗底 + 窄亮芯），密集处靠 `lighter` 叠加自然变亮而不是调亮单点（这正是 deck.gl 85 万点好看的原因）。**已否决**：光柱/Beam（实测光柱长时变「刺猬」且 2D 平面不成立）、热力图（抹掉「一个人=一束光」的核心隐喻，且正是 Strava 泄露事件的形态）、六边形聚合柱（人造几何与两种自然意象都冲突）、Mapbox/CARTO/Stadia 深色底图（视觉最好但中国大陆合规不成立，只能当调色参照）。**两条反对意见**：①实测夜光图上青海西藏新疆**几乎全黑**，西部用户打开看到自己身处一片黑暗，与「你不孤单」正相反 —— 需产品方看过实测图自己判断；②本篇全程在讨论「好看」，而低视力用户需要**高对比**，深色底 + 低 alpha 辉光大概率不过 WCAG，若受众含低视力需另做高对比模式且会牺牲全部精致感（本轮未量化验证）。**未做**：陪伴感的心理学依据、Mapbox/MapLibre/腾讯地图、10k/100k/1M 三档性能量化、location fuzzing 具体算法（`k=5` 仍无出处）、夜光影像等距圆柱投影与高德 Web 墨卡托/GCJ-02 的重投影验证 | 产品方看过西部全黑的实测图后否掉方向 C；「卫星影像底图 + 自有标注」拿到官方定性（任一方向都会改写合规路径）；NASA 改图像使用条款或下架 Black Marble 下载页；受众确认包含低视力用户（需补高对比模式研究）；产品改为只做语音播报不做视觉版（本篇整篇作废）；高德自定义样式平台改样式格式或下线内置 dark 主题 | [light-map-visual-directions-20260911.md](./light-map-visual-directions-20260911.md) |
| 2026-09-13 | 🔚 **2026-09-23 已被「星火同行」替代**（见本表 09-23 那行；PR #174 关闭，分支留作存档）—— 「一束光地图」**技术架构与位置隐私**（第三部分，补齐原需求的 D 与 F；前两份管「能不能做」与「做成什么样」）：Mapbox/MapLibre/腾讯三家 SDK、图层能力矩阵、10k~1M 性能与流量、k 值依据、模糊化算法、点亮动画的隐私代价 | 🔴 **推翻上一轮两条**：①`k=5` 是错的方向（它只在低风险/单一属性/大基数数据集里被引为下限，我们三个前提全不满足），按「污名化+高敏感」应取 **k≥20**；②「新用户点亮动画」**本身就是隐私泄露通道** —— Sweeney 2002 §4.3 时序攻击逐字「linking the tables RT0 and RTt may reveal sensitive information and thereby compromise k-anonymity protection」，增量为 1 时 k 取多少都当场失效，**这是该交互的固有属性不是实现缺陷**；改法三选一，最优是**做成给注册者自己看的一次性动画**（情绪价值全保留、零隐私代价）。**k 值找不到可照抄的权威数字**：Sweeney 原论文从未推荐任何 k（全文只用 k=2 教学）；各国官方用过 3(Census FSRDC)/10(ONS)/20,000人(HIPAA)/250,000人(CPS)，**GB/T 35273-2020 全文零量化数字**（负向结论），GB/T 37964 同样只给模型不给 K。🔑 **HIPAA 45 CFR §164.514(b)(2) 的真正含义是「低于阈值要归并不是隐藏」**（低于 2 万人的地理单元改写成 000），ONS/Census 同构（粗粒度化+抑制，**不用噪声注入**）⇒ **自适应粒度比 k 取多少更优先**：自算模拟 k=20 下，固定区县级在 3000 用户时只覆盖 **25.7%** 的人（74% 的用户在地图上不存在），而自适应（县→市→省逐级归并）同规模下 **100%** 覆盖；产品拍板的「放大到区县」应理解为终态上限不是固定粒度。加噪声路线整体出局：Geo-indistinguishability 作者自己写「a level of privacy that **scales linearly with n**」，我们是静态位置无限次重复展示。架构红线（Tinder 2013/Bumble 2021 三边定位漏洞的行业原则原文）：**聚合必须在服务端，客户端不得拿到明细坐标**。Strava 2018 后**并没有修好**（只加了三个补丁，机制没变，2023 年 NCSU 仍反推出住址）⇒ 根因是粒度固定，打补丁救不了。敏感认定**两条腿都站得住**：PIPL 第 28 条明文列「行踪轨迹」、GB/T 35273 表 B.1 明文列「精准定位信息」（均[高]），视障身份那条只到转述层面（《敏感个人信息识别指南》的「特定身份信息」含残障人士身份，openstd 无免费全文）。**行业空白**：没找到任何同类产品对「残障用户位置」做过比普通用户更严格的技术处理，无先例可抄。技术侧：**Mapbox 直接出局**（无测绘资质，Mapbox.cn 2022 下线）；**MapLibre** BSD-2 可商用但瓦片源合规存疑（高德/腾讯是否允许第三方引擎消费矢量瓦片**无官方条款**，须商务书面确认）；**腾讯**有甲级资质 + **免费的「微信深色模式同款地图」模板** + SDK 展示无调用上限，是唯一值得记的备选。🚩 **订正一条转述错误：高德 iOS 内置夜间底图，一行 `mapView.mapType = .standardNight` 即可**（本机头文件 `MAMapView.h` 原文有 `MAMapTypeStandardNight` 与 `MAMapTypeNaviNight`），不必去自定义样式平台 —— 上一轮转述说「必须后台建样式」只对自定义样式成立。海量点约束**比此前记录更严**：`items` 是 **readonly**（整个数组都换不了，只能重建 overlay），renderer 只有一个 `icon` ⇒ **两类用户必须拆成两个 overlay**；意外收获是海量点**支持点击**（`didItemTapped:`）。**性能问题被「聚合」消灭了**：全国县级行政区 **2842 个**（民政部代码表逐条解析，非二手数字）⇒ 客户端点数上限 2842，与注册用户数无关，远低于高德 10 万背书；实测 gzip 流量**恒定 57 KB**（不聚合时 1M 用户要 8.3 MB），且完整字段名 vs 缩写 gzip 后只差 4 KB（不值得为省流量牺牲契约可读性）。**结论：不换 SDK**（换要动冻结的 Podfile + 重做定位/逆地理/POI/轨迹四块，而技术优势在 2842 点的量级上体现不出来）。**未做**：真机帧率与功耗（CI 跑不了 XCTest）、A 陪伴感的心理学依据（唯一能回答「值不值得做」的一块） | 后端上线分布聚合端点（k 与粒度策略落地时回来对照本篇）；产品坚持「新用户注册全体可见地点亮」（须改为明示+可退出，且不得再声称做了 k-匿名）；真实用户分布远比模型集中（自适应会大量回退到省级，应改做「你周边」局部视图而不需要审图号）；高德/腾讯就第三方引擎消费矢量瓦片给出官方答复（决定 MapLibre 路线）；高德 iOS SDK 改 `MAMapType` 枚举或让 `MAMultiPointOverlay.items` 可写；拿到 GB/T 37964 或《敏感个人信息识别指南》官方全文；k 值需要下调时先看本篇 §2.3 的覆盖率代价 | [map-tech-and-location-privacy-20260913.md](./map-tech-and-location-privacy-20260913.md) |
| 2026-09-15 | 盲人端五屏（首页/等待/汇合/跑步中/完成评价）的参考驱动设计：BeMyGuide、Sense、Be My Eyes、Strava/NRC 四个参考各该借什么、不借什么 | **盲人端的「大」不是把按钮放大，是让屏幕上不存在非控件区域** —— Sense 的跑步中界面是整屏切成等分色块、中间深色分隔带、**零留白零边距且一个数字都没有**，与「64pt 按钮 + 常规布局」是两种东西。🔴 **材料性质必须先分级**：指定的两个主参考（BeMyGuide 2015 黑客松 / Sense 2019 Adobe Jam）**都是概念稿从未上架**，按本仓库既有结论属「只看视觉方向不做 UX 决策」那一类 ⇒ 只取其**一手用户引语**与**结构原型**，任何「所以该这样做」的效力由上架产品背书。三个参考独立收敛到一条反直觉决策：**不要让用户给「人」打分**（BeMyGuide 原文 `rate run experience rather than particular person`；Be My Eyes 评价 `will not be shared with another party`）⇒ 契约的 1–5 星不改，改的是评价对象措辞与视觉隐喻（评「这次陪跑」，刻度用跑步小人不用星星）。🔴 **Strava/NRC/adidas 三家无障碍全部不及格**（AppleVis 实测 Strava `VoiceOver 只念 Button, button, button`；NRC「开发者尚未注明支持的无障碍功能」；adidas `mostly inaccessible`）⇒ 它们**只能借视觉与情绪语言，交互模式一条都不能借**，「Strava 怎么做的」在本仓库不构成无障碍论据。BeMyGuide 一手引语揭出一条**真实产品缺口**：「Hearing another person's voice helps me to get an idea of their character」⇒ 盲人评估陌生人靠声音不靠照片，它的解法是**注册时录一次的异步语音自述**，而我们用 `PENDING_INTRO_CALL` 一通真实电话解决同一需求，成本高得多且有 3 轮窗口耗尽风险（属新能力需后端契约，本篇只记证据）。设计语言**不发明新色板**（`AppColors` 已有 WCAG 实测值 + 四组合测试），只新增一个缺失的**行动色** `#D7FF3E`（现有四色全是语义色，拿 `success` 绿做主按钮等于此后无法表达「成功」）：黑字 18.3:1、**白字 1.15:1 绝对禁止**、浅色模式下必须描边否则块边界消失。方向选 **Athletic & Bold**，「暖」由**内容层**承载不由颜色层 —— 理由是本仓库已两次被「好看的颜色」坑过（`systemGreen` 压白底 2.22:1、`#30D158` 配白字 2.02:1）且低视力通道从没验收过。🚩 **通用跑步 App 模板的 `Paused` 状态在本项目不存在**（11 个状态里没有），不要凭模板加没有业务语义的控件。**已否决**：纯黑白（11 个状态没法只靠文字分级）、底部 tab（盲人端只有一条主线且底部被求助条占了）、Sense 的自建长按朗读（与 VoiceOver 手势冲突）、adidas 的 `SLIDE TO PAUSE`（Uber Base 官方 Caution：动作不关键时滑动徒增复杂度）、首页模型 B/C（新用户首屏全是零 / 首个焦点是对全盲价值为零的头像列表） **第二轮（同日 · 精炼，输入是「保留黑+柠檬绿+持久求助条」）**：🔴 **`AGENTS.md` §6 把产品决策写成了技术约束** —— 原文「两端入口只在 `IN_PROGRESS` 开放（`EmergencyTriggerRequest` **必须带 `orderId`**）」，而契约原文逐字是「三个字段**全部可选**。不传 `orderId` 即独立 SOS（无进行中订单也能求救）」（`api_spec.yaml:5471-5473`），participant 校验只在传了 orderId 时才做 ⇒ 因果链断了，闸门其实在客户端（`SafetyModule.swift:354-359` 的 `guard let order … else { .localCall }`）。**关着的真实理由只剩「`IN_PROGRESS` 是唯一保证握着新鲜真实 GCJ-02 坐标的状态」**，注释里另一条「backend participant check」已作废。危害不是措辞不精确而是**没人会去问产品**：读到「必须带 orderId」的人认为独立 SOS 做不了，于是永远不上议程 —— 而同一节下面五行的坐标那条从一开始就诚实写着「后端技术上接受…在产品/安全批准前保持 false」，两条同构一条不诚实。已订正 230 行为同一种诚实度，**红线本身不动**。建议维持闸门关闭，但理由换成坐标而非「技术不支持」；打开前要回答坐标从哪来、60 秒冷却按触发者计的误触代价、以及 `enable-independent-sos-safely` 里 **4 条真机验证欠账**（35/42 完成，未完成的全是设备离线阻塞，该功能从没在真机上验过）。**订正第一轮 mockup 两处错误**：①「求助」是保留词专指云端链路，非 `IN_PROGRESS` 的正确标题是**「紧急呼叫」**（`homeCallTitle`），用错词会让看不见屏幕的人以为求助已发出；②跑步中不是单个红块而是垂直三层，且直接拨号**常驻不等失败才出现**（Apple 5.1.5 要的是按得到的入口）。交互模型五选一取 **B（单击→确认→执行，也是现状）**，**C/D 长按是技术性否决**（VoiceOver 开启时长按语义被系统接管，要 double-tap-and-hold），B 成立的关键是**误触代价两端不对称**（盲人能自撤、志愿者恒 403）；E 菜单与「空间记忆」原则自相矛盾。⛔ **「联系陪跑员」不能进求助菜单** —— 一对一陪跑里志愿者可能就是威胁来源；**「联系客服」也不做** —— 7 个客服端点全在 `cs-controller` 下由客服侧调，没有面向用户的联系客服端点。新增第 11 条原则**动静分离**（一个焦点里不放「会变的」+「不变的」）⇒ 姓名与距离不合并、跑步中选「距离优先」否掉「搭档优先/状态优先」。建议把本地拨号从实心红改**描边红**，让饱和度编码后果的不可逆程度（未验 AX5 与户外强光）。 | 对标产品改版（Be My Eyes 派单或评价机制、Strava/NRC 主指标层级）；`#D7FF3E` 跑完 `AppColorContrastTests` 四组合或真机/低视力用户实看后被推翻；后端上线语音自述契约；本仓库再动盲人端首页或订单页信息架构；`PENDING_INTRO_CALL` / `SCHEDULED_CONFIRMED` 两屏要补进状态系统时 | [blind-runner-ui-reference-study-20260915.md](./blind-runner-ui-reference-study-20260915.md) |
| 2026-09-19 | Claude 的「agent view」能不能承接本项目的跨仓库长任务（前端 + 后端一起给一个任务做到完成）？现在拖着没做完的是哪些？ | **能力是真的、跨仓库也真能做，但它解决不了本项目 64% 的积压。** 🔑 **订正一条只查文档会查错的事实**：官方文档里「跨两个 git 仓库」查不到（Agent Teams 无多仓库设计、cloud sessions 逐字 "`--cloud` works with a single repository at a time"），但本机 `claude agents --help` 逐字写着 `--add-dir <directory> … (repeatable)` ⇒ **agent view 分发出去的每个背景会话都能同时挂 iOS 仓库与后端 `demo`**，与 `CLAUDE.md` 已有的双挂载同机制、只是从一个会话扩到一屏。与 `AGENTS.md`「不用 Agent Teams」不冲突（那条针对 teammate 不继承 cwd，Agent View 是另一个东西）。⛔ **`/goal` 那一段整块作废（2026-09-21 订正）** —— 原文断言「`/goal` 不存在、全局 `CLAUDE.md:16` 是死引用、官方文档也查不到」，**三句全错**：`https://code.claude.com/docs/en/goal` **HTTP 200 / 16,657 字节**，官方 Automation 分类，标题逐字 "Keep Claude working toward a goal"。🔑 **判错的根因值得单独记**：三处核实（`claude --help` / `find -iname "*goal*"` / session skill 列表）**全都打不到「内置斜杠命令」这个类别** —— `/clear`、`/compact`、`/effort` 同样都不在，**三个打不到的地方零命中 = 零信息，却看起来像三重确认**；而该报告**自己的数据就自证了方法无效**（第一条写 grep `"goal|loop"` 零命中，第三条却写 skill 列表里**有** `loop`）。🔴 **传播链是本仓库「PR 被关掉」的第一例可量化损害**：正确的官方 URL 一直写在 `opus5-workflow-and-effort-20260906.md` §C.3 的来源行里，但那份报告在**已关闭的 PR #108** 分支上、不在索引里 ⇒ 09-19 照 §12 第 1 条读了索引没找到、重做一遍、得出相反结论 ⇒ 09-21 又被我引用放大（据此说另一个 PR「把不存在的命令教进主线」，还建议改掉全局配置里那条**本来正确**的规则）。⇒ 被关掉的 PR 的损害不是「那份工作白做了」，是**它的正确结论缺席之后后来的人会填进一个错的**，而错的会进索引被引用（同形状先例见 `web-design-advice-is-mostly-not-for-swiftui`），要「给条件迭代到满足」现成写法是 `/loop` + prompt 里写死判据。**七项能力无一是「给定验收条件自主迭代直到满足」**，最接近的 `/loop`（重复同一 prompt）与 Workflows（JS 脚本 fan-out，默认并发 16 / 单次 `parallel()` 4096 / 单轮 1000 agents）收敛条件都得你自己写死。🔴 **真正的判断在积压这一侧**：Issues 在本仓库是**关闭**的，待办在三处 —— 14 个未归档 OpenSpec 共 **42 条未完成**、**30 条孤儿远端分支**、2 个 CI 全绿却没合的 PR；而 42 条逐条读下来 **≈27 条（64%）是真机人工验证**（开 VoiceOver 听读回、AX3 字号目视、「真机批跑 —— 设备 `111` 本轮全程离线」），最老的 `enable-independent-sos-safely` 从 2026-07-10 拖到现在 71 天、7 条未完成**全部**是设备阻塞，SOS 至今没在真机上验过 ⇒ **积压卡在真机验证通道，不卡在 agent 吞吐**，自主 agent 的天花板就是「编译过了、PR 开了」，再往前一步就是记忆 `merged-prs-whose-tests-never-ran` 记的那个事故（CI 绿在本仓库只是编译信号）。⇒ 判据是**验证依不依赖真机**：值得派的是孤儿分支判活（⚠️ squash 合并让 `branch --merged` 误报）、openspec 记账对账、跨仓库契约对撞；**不要派**那 27 条真机验证 | Claude Code 给 agent view 或 cloud sessions 加多仓库支持；`/goal` 真的出现在官方文档或 `claude --help` 里（届时全局 `CLAUDE.md:16` 从死引用变回有效）；Agent Teams 让 teammate 可指定 cwd；本仓库拿到可用的模拟器通道或自动化真机验证（**这一条一发生，整篇「不该派」的结论直接反转**）；Workflows 的并发/规模上限变化 | [claude-agent-view-for-cross-repo-20260919.md](./claude-agent-view-for-cross-repo-20260919.md) |
| 2026-09-16 | 锁屏实时活动（D 组）：按钮点击后能不能**不打开 App 就发声**？后台震动受什么限制？（设计包 README「落地顺序建议」第 5 条点名要先写 demo 验的两件事） | **能发声，且不需要 `UIBackgroundModes: audio`。** 手写两 target demo 跑模拟器实测：锁屏按钮 → 系统在 **app 进程**里执行 `AudioPlaybackIntent`（日志 `bundle=com.zwaidrun.lademo process=LADemo`）、App 不打开、屏幕不亮，`AVAudioSession` 激活成功，`didStart`/`didFinish` 都回调；把 `UIBackgroundModes` 整键删掉重装，行为不变 ⇒ **不要给主 App 加 audio 后台模式**（多余且会被审核问）。🔴 **一条坏消息必须原样记住**：官方文档逐字「On a locked device, buttons and toggles are inactive and the system doesn't perform actions unless a person authenticates and unlocks their device」—— Face ID 机型上「看一眼」即完成认证（人仍停在锁屏、按钮可用），但**手机在臂带/口袋里、脸没对着屏幕时按钮不响应**，而那正是盲人跑者的常态。模拟器没有密码锁，复现不了该状态 ⇒ **留作真机人工验证**（遮住原深感摄像头再按）。协议选择是硬的：裸 `AppIntent` 在 **widget 进程**里执行、没有音频会话，按下去一声不响；只有 `AudioPlaybackIntent` / `LiveActivityIntent` 才在 app 进程跑（`Activity` 16.1+ / 两个 Intent 协议 17.0+，`ActivityContent` 16.2+）。意图类型必须**同时**编进 app 与 widget 两个 target（app 是执行处、widget 是 `Button(intent:)` 的编译依赖），而 widget target 编译不到 `AppColors`/`FlowMetrics`/`VoiceService`/`TrackStats` ⇒ 取值与 utterance 策略各成第二份，靠 `RunLiveActivityTests` 对撞回钉。顺带四条会浪费下一个人时间的实测：①**重装 App 会杀掉正在进行的实时活动**（看起来像代码把它弄坏了）②系统在锁屏上问**两次**授权（第二次才是 Always Allow）③**卡片主体是深链接**，点在按钮以外任何地方 = 打开 App（盲人高频误触面，VoiceOver 逐元素划过去不受影响）④**卡片高度上限约 175pt**，按钮从 52 拉到 110 时顶行当场被裁且无任何警告 ⇒ 全屏那套 82/36 字号放不进来，只能用状态清单 §16 的 56/24/15/13 | iOS 大版本改实时活动的交互授权口径（尤其「locked device」那句）；Apple 调整 `AudioPlaybackIntent`/`LiveActivityIntent` 的执行进程；实时活动高度上限变化；阶段 2 要依赖后台震动时（本轮只证明了「没崩」，模拟器没有振动马达） | [ios-live-activity-lock-screen-20260916.md](./ios-live-activity-lock-screen-20260916.md) |
| 2026-09-17 | Claude Code 在本仓库用量过快（Max + Opus 5，改两个界面耗掉 5 小时额度 30%+）：token 花在哪、官方与社区各推荐什么解法 | 🔴 **「常驻上下文 × 每一轮」的成本模型是错的** —— 官方 prompt caching 文档逐字 cache read「billed at roughly 10% of the standard input rate」，CLAUDE.md 类内容是**全价一次 + 每轮一成**，删它的理由是窗口占用与信噪比、不是账单。真正贵的是**缓存失效**（一次 = 整个会话按全价重读），官方给了 9 条失效动作的完整清单，其中三条本仓库天天碰：中途改 effort、`/compact`、**图片累积到上限被批量丢弃**（iOS 截图验证正落在这面）。🔴 **社区最热门的修法 `ENABLE_TOOL_SEARCH=false` 机制是反的** —— 官方明文 deferred tools 变化「only appends new content and doesn't disturb anything already cached」，关掉等于把 298k 工具定义搬回前缀再让 server 抖动炸缓存；那条建议的真实出处是 2.1.69 的 `defer_loading`+`cache_control` **400 崩溃**（#30920/#30989）的 workaround，被转述成了通用省钱建议。🔑 **诊断有权威判据**：`/usage` 的 `Prompt cache (main)` 行给命中率/miss 次数，v2.1.260+ 还点名 miss 原因（如 `likely cause: tool definitions changed`）—— 不要再估算。**subagent 默认只有 5 分钟 TTL**（主对话是 1 小时），Opus 5 high effort 下极易超时重读，修法一行 `subagentPromptCacheTtl: "1h"`；fork 能读父缓存、subagent 不能。官方方向证据最硬的一条：Anthropic 给 Opus 5 **砍掉了 80%+ 的系统提示词且编码评测无可测退化**，并逐字点名「CLAUDE.md 当成所有实践的中央仓库」是 myth，要用 progressive disclosure —— 而 skill 注入**不进前缀**（作为 user message 追加）⇒ 搬进 skill 是纯赚。本仓库三个特有放大器：`AGENTS.md` §10.6 强制全文读（`BlindOrderStatusView.swift` = 51k tok）、§12 强制读 `INDEX.md`（本文件 = **29k tok**）、`research-log.mjs` 每次联网调用重复注入 12.1KB（本轮 4 次 ≈ 16k，纯重复，是缺陷）。**已否决**：`permissions.deny` 屏蔽 Pods/.build/worktrees（ripgrep 尊重 `.gitignore`，实测 220 vs 25483，搜索面本来就干净）、清 14G worktrees（占磁盘不占 token）、全面降级 Sonnet（官方证据是「少指令不降表现」不是「小模型不降表现」）。⚠️ **最大空白**：只拿到 `/context`，`/usage` 与 `/mcp` 未提供 ⇒ 「本例究竟是不是缓存失效」**未证实** | Claude Code 升级后 `/usage` 的 `Prompt cache (main)` 行或 `subagentPromptCacheTtl` 语义变化；#40314（HTTP MCP 不 defer）修复或确认适用本机；官方改 cache read 计价比例或 TTL 分桶规则；本仓库 `AGENTS.md` §10.6「改前完整读」或 §12「开搜前整份读索引」改口径；auto-memory 与手工 `memory/` 目录的关系被官方澄清 | [claude-code-token-optimization-20260917.md](./claude-code-token-optimization-20260917.md) |
| 2026-09-20 | 会话结束自动归档 + 下次按需召回记忆，不重复输入上下文：官方能力 / 社区实现（claude-mem）/ 能不能接 NotebookLM | **缺的不是存储是召回的触发条件 —— 归档这件事三个月前就做完了。** 🔴 **claude-mem 不用调研该不该装：这台机器上装过、跑了 111 天（2026-04-09→07-29，v12.4.3）、然后卸了** —— `sdk_sessions` 163 条**全部 `status=completed`** 而 `observations`/`session_summaries` **双双为 0**，日志里 **2289 次** `SDK session poisoned — killing and respawning {outputClass=prose}`，残留 81MB（69M 是失败日志）；它用 `CLAUDE_MEM_MODEL=claude-sonnet-4-6` + `auth=cli` **烧你自己的订阅额度**做摘要（⚠️ 但它 08-03 就停了，与 09-17 报的额度问题差 45 天，**不能栽赃**）。上游其实很健康（94,252★、Apache-2.0、调研前一天还在 push、v13.24.23），⇒ 🔑 **star 数不是证据、本机实测才是**（同型见记忆 `external-skill-may-lower-your-standard`）；⚠️ 严格说只证伪了 v12.4.3，翻案判据现成且便宜：重装跑一周查 `observations` 表有没有非零行。三个对口 issue：[#2579](https://github.com/thedotmack/claude-mem/issues/2579) 安装时写 `CLAUDE_CODE_DISABLE_AUTO_MEMORY=1` **卸载不还原**（⇒ 静默关掉原生记忆，**本机四处配置已核实未命中**）、[#3905](https://github.com/thedotmack/claude-mem/issues/3905) 泄漏 808 对孤儿 `chroma-mcp` 进程吃掉 74GB swap、[#1848](https://github.com/thedotmack/claude-mem/issues/1848) 「会话一开始 40% token 瞬间消失」；「HN 讨论热烈」是**假线索**（Algolia 核实那帖 0 评论 1 分）。**本机已有四层记忆**：auto memory（71 条，🔑 **官方原文确认 `~/.claude/projects/<proj>/memory/` 就是官方 auto memory 的默认路径、手工编辑是官方预期用法、不存在两套并存也不双份计费** —— 补上 `claude-code-token-optimization-20260917` §8 的空白第 3 条）、自建 board 看板（390 会话/本项目 130，Stop 采集 + SessionStart 注入最近 3 个 × 900 字符；⚠️ **别改挂 SessionEnd**，Stop 在会话被强杀时数据已落盘而 SessionEnd 只有约 1.5 秒预算）、`docs/research`+`docs/review`（38 篇，进 git 永久）、transcript 全文检索。🔴 **transcript 默认 30 天清理且静默**（本机最老只到 08-03）⇒ 搜 08-21 的 `ITMS-91053` 召回的全是 9 月读过索引的会话，**原始那次已经没了** ⇒ **transcript 是 30 天缓冲不是长期记忆**，这反过来把 §12/§13「必须落盘」的理由变得更硬。`search_session_transcripts`（桌面端 MCP，CLI 文档查不到）**实测三次可用且跨项目**，但判据是：**查询词若出现在每会话注入的大文档里（`INDEX.md`/`AGENTS.md`/`MEMORY.md`）结果全是回声**（搜 `MAMultiPointOverlay` 五条 snippet 逐字相同），要用只可能出现在对话里的词（错误签名/具体数字）才精准（搜 `Test crashed with signal kill` 四条各不相同）。**NotebookLM 连不上且方向是反的**：消费者版无官方 API 无官方 MCP，官方 API 只在 Gemini Enterprise（Pre-GA，$9–45/人/月），第三方 MCP 全是浏览器自动化/逆向 `batchexecute`，**star 最高的 3,432★ 调研当天已归档停更**，第二名作者自己建议「use a dedicated Google account」；更关键是它**进得去出不来**（只能导到 Google Docs/Sheets + WAV，**聊天历史零导出通道**、引用 chip 复制即丢），而召回要求取得出来 ⇒ 要外部研究库直接用 **`~/Documents/Obsidian Vault`（已挂在会话工作目录，零配置可读写）**，比 obsidian-local-rest-api（v5.0 起内置 MCP，本机未装）/ Notion 官方 MCP（需交互式 OAuth）都省。**已否决**：调大 `cleanupPeriodDays`（治不了病还加重噪声，该长期留的东西应主动落盘）、再装第五层记忆（解决不了「前四层没人触发」，且都要抢同一批 hook 挂载点）、API 侧 memory tool（`memory_20250818` 是给自己写 agent 的人用的，CLI 用不上）。⚠️ **一条差点报错的发现**：`MEMORY.md` 32.5KB **字节**超官方 25KB 上限，按算术推断末 15 条被截断 —— 而对照本会话实际注入内容**没有截断**（上限大概率按字符计，中文容量约 3 倍），同型见记忆 `reviewer-numbers-that-match-a-red-line`。**顺带一个当场成立的缺陷**：`codegraph prompt-hook` 挂 UserPromptSubmit，本会话（纯调研、零代码改动）注入 **3 次 55.3KB ≈ 14.2k tok** 全是无关 iOS 业务符号，**其中两次是 subagent 回来的 task-notification 触发的**（那根本不是用户提问）⇒ 是分流缺陷不是工具缺陷，**别关 codegraph**。**唯一真争议**：AI 该不该自己决定记什么 —— 官方方向是自动记忆，而 HN 有实名用户批评它「records severely wrong statements that then would confuse it later」，本仓库 71 条人工审过带出处的记忆质量明显更高。**附本次调研自己的成本基线（以后定档直接拿这个对照）**：一次 C 档三线并行 = subagent **483,728**（192k+148k+144k，占 **60%**）+ 主会话 315,277 ≈ **800k tok = Max 5x 五小时额度的 13%**；🔴 **subagent 不省额度只省窗口**（读不到父会话缓存、每个独立全价、报告回传又整份进主上下文）⇒ 要省额度只有「不派」；**先盘点再定档可省 ~37%** —— 本轮真实失误是先开三线、回头才查本机，而本机五条命令就把 claude-mem 那条线的答案全给了（那 148k 本可不花）。已落回全局 `~/.claude/CLAUDE.md` 委派节 + 新增「≥3 个 subagent 或预计 >200k 先报预算」 | claude-mem 新版本修掉 `SDK session poisoned`（重装跑一周查 `observations` 非零即翻案）；**你的重复输入其实是跨项目而非跨会话**（四层里三层按 repo 隔离，唯一跨项目的 transcript 检索只有 30 天 ⇒ §8① 整节反转）；Claude Code 改 `cleanupPeriodDays` 默认值或 transcript 存储格式（`scripts/hooks/transcript.mjs` 正在解析它，官方明说「changes between versions」）；官方给 auto memory 加跨项目召回或加语义检索；NotebookLM 对个人账号开放 API，或你所在组织买了 Gemini Enterprise；`search_session_transcripts` 从 substring 改成语义检索；本仓库不再每会话注入大索引（§2.1 的噪声判据随之作废） | [claude-code-memory-and-session-archival-20260920.md](./claude-code-memory-and-session-archival-20260920.md) |
| 2026-09-07 | AI 辅助的 SwiftUI 界面设计：有没有好用的 skill、界面「设计得没想象中好」怎么系统性改善、开发流程该是什么 | **缺的不是工具是流程**。官方 `frontend-design` 的 SKILL.md（71 行）**框架无关部分占 96%**，只有 3 句 Web 专属（`For web designs, the hero…` / `CSS selector specificities` / `visible keyboard focus`）——但**别装它，抄它的两遍工作法**：先出 token 计划 → 对着 brief 自审「这是不是我对任何同类页面都会产出的默认值」→ 才写码 → 截图自评（原文 "a picture is worth 1000 tokens"）。它的 `## More on writing in design` 整节对本仓库价值高于视觉部分（语音优先 App，"Errors don't apologize, and they are never vague"、动作全流程同名）。**争议要留档**：HN 上 rafram 说它 "placebo at best"、efficax 说「一眼认出用了这个 skill」、k2so 实证它**自己**产出暖米色新指纹（而当前 SKILL.md 已把 `#D97757` 写进自己的破绽清单——skill 在追着自己的输出打补丁）⇒ 官方那份 AI 指纹清单是 Web 语境且在快速过时，**照抄它是错的，iOS 版清单外面不存在必须自己写**（Inter/Roboto 禁令对 iOS 无意义，默认字体是 SF；玻璃拟态在 iOS 26 是官方方向，与 Web 结论相反）。**推翻 09-02 的「iOS skill 生态位基本空着」**：`ui-ux-pro-max`（125,726★/MIT，`swiftui.csv` 标 iOS 16+ 基线、`pro-rules.md` 117 行是真 native 检查单）、`rshankras/claude-code-apple-skills`（709★/MIT，183 个 SKILL.md）均真实可用——**但标准低于本仓库**（它们要 44pt 触达 / 我们盲人端要 64pt；`AppColors` 已按 WCAG 四组合实测 4.5:1 并有自动测试），整装是引入更松的标准 + Axiom 同型的上下文成本，**只值得取 `pro-rules.md` 里本仓库没有的 3 条**（图标对比 3:1、按下/禁用态在明暗两套下的可区分性、safe-area 与底部常驻求助条）。`dickwu/apple-design-skill` **无 license 不用**。**本仓库真正的三个缺口**：🔴 没有审美方向声明（`ui-handoff-ios.md` 67.5K 只讲「这页要有什么」，两遍工作法的第一遍根本不存在）；🟡 间距/圆角/动效**无 token**（颜色字体有且质量高，但 37 个视图 1169 处视觉常量硬编码，`enum Spacing\|Metrics\|Layout` 全仓 0 定义——**已装的 `swiftui-pro:design.md:5` 自己就在建议这件事**）；🟡 截图闭环未验证且拍错地方（导出代码 `b37e71b` 昨天才落地、本机零 `.xcresult`；`attachScreenshot` 实测 10 处**全在 `blindRunUITests.swift`，`AccessibilityAuditTests.swift` 一处没有** ⇒ 最该拍的横屏/AX5 用例跑完不留图）。**通道结论**：XcodeBuildMCP 的 `screenshot`/`snapshot_ui` 官方 JSON 写死只在 `ui-automation` 分组且 "for iOS simulators"（本会话可用工具列表逐字印证），真机拿不到截图 ⇒ 现有 `xcresulttool export attachments` 就是对的那条路（且**不在 Xcode 16 废弃名单里**，废弃的是 `get/export object`；Apple 自己的废弃提示有两处 typo）；⚠️ 它**没有匹配附件时 exit code 仍是 0**，判成功只能查文件数（`device-test.sh:190` 已这么做，别处复制会漏）。🆕 观望 Xcode **26.3** 的官方 `xcrun mcpbridge`（本机 26.2 实测该命令不存在），但 `RenderPreview` 受 GUI/TCC 门控无头不可用，**且「能否跟随真机」是推断不是事实**。**最值钱的风险**：所有通道共同盲区不是「拿不拿得到图」而是「拿到的是不是生产形态」——UI 测试构建永远没有高德 key，地图恒为占位图，任何看图结论必须附「测试构建/降级路径」。已否决：`swift-snapshot-testing`（真机 sandbox `Operation not permitted`，issue #379 至今 open，且其变通方案就是我们已有的做法）、`ViewMonitor`（人工点按无程序化输出、仅 Debug）、Figma MCP（Xcode 27 beta + 方向是设计→代码） | 本机 Xcode 升到 26.3+（要重验 `xcrun mcpbridge` 的 `RenderPreview` 能否跟随真机——这条一旦成立，§4 整节结论要重排）；模拟器通道恢复（高德出 arm64-sim slice）；官方 `frontend-design/SKILL.md` 再次改写（它已被证明会追着自己的输出打补丁）；`ui-ux-pro-max` 或 `rshankras` 把触达/对比度标准提到本仓库水平以上；Apple 公开 Xcode MCP 的工具清单 | [ai-ui-design-workflow-for-swiftui-20260907.md](./ai-ui-design-workflow-for-swiftui-20260907.md) |
| 2026-09-21 | Figma 适合做本项目的 iOS UI 设计吗？能不能把 Claude Code 和 Figma 连起来？（09-07 §2.4 把它否掉了，还作数吗） | **一半作数，一半是误读，而误读的是决定性的那一半。** 09-07 引的原文今天仍逐字成立（Figma help center："The Figma MCP server is only supported in the Xcode 27 beta app"），但它把这条当成**整条路**的门槛 —— 那句只约束「**在 Xcode 里**用」。Figma MCP 是远程 HTTP server `https://mcp.figma.com/mcp`，**Claude Code 自己就是 MCP 客户端，不经过 Xcode** ⇒ 那条否决对我们这条路不适用（同型见记忆 `symptom-match-is-not-mechanism-match`：**引文正确不等于推论正确**，引限制条款要问「它约束哪个组件」）。第二条否决理由「方向只能设计稿→代码」**已被官方推翻**：`figma/mcp-server-guide` 的 `figma-swiftui` skill frontmatter 逐字 "in EITHER direction"，并有独立 `references/code-to-design.md` 讲**把 SwiftUI views/screens/tokens 推回 Figma**，细到 `List` 分组末行不画分隔线、`Spacer()` 要落成 `SPACE_BETWEEN` 而非实体节点、`VStack/HStack` "never absolute x/y"；HIG 语义色逐字 "Never paste a hex" 并给 `Color(.systemBackground)`↔`background/primary` 完整映射（🔑 正好对上本仓库语义化的 `AppColors`）；Apple 官方以 Community library 发布 *iOS 18/iPadOS 18* 与 *iOS and iPadOS 26*，含 **Product Bezels**（iPhone 16 = 393×852 / 16 Pro = 402×874，逐字警告混用宽度会错位）。🔴 **门槛换成两条都不是技术问题**：① `README.md` 逐字 Starter 计划或 View/Collab seat **每月只有 6 次工具调用**（任何实际工作流下等于不可用，要 Dev/Full seat on Professional+）；② write-to-canvas 仍是 **Beta** 且官方明说 "will eventually be a usage-based paid feature"。⇒ 三种用途判定不同：**画稿给人看**✅ 直接用且根本不用接 MCP（导出 PNG 可直接 Read）；**稿→代码**🟡 可试但不是瓶颈（瓶颈是没有稿也没有审美方向）；**现有 SwiftUI→Figma**🟡 最有价值（「说不出哪儿丑」只能目视，见记忆 `flexible-spacer-steals-half-the-row`）但要先算 seat。⚠️ **本轮没有实际连接过 Figma MCP**，「能做」全部来自官方文档与 skill 原文，属「已核实文档、未验证行为」；且 09-07 的核心结论「缺的是流程不是工具」**未被推翻**，Figma 换掉的只是四步流程第 4 步的载体。本机 Xcode 仍 26.2、`xcrun mcpbridge` 仍不存在（14 天内没变） | 本项目 Figma 账号拿到 Dev/Full seat（届时 §3 从「文档说能」升级为「该实测」）；write-to-canvas 结束 Beta 或开始计费；Figma help center 改口支持稳定版 Xcode；本机升到 Xcode 26.3+（重验 `xcrun mcpbridge`）；真的跑过一次 `use_figma` 之后（本篇全部结论需从「未验证行为」升级） | [figma-mcp-bidirectional-20260921.md](./figma-mcp-bidirectional-20260921.md) |
| 2026-09-21 | 🔚 **2026-09-23 已被「星火同行」替代**（见本表 09-23 那行；PR #174 关闭，分支留作存档）—— 「一束光地图」改成**只到城市级、不下钻街道**之后，能不能免地图送审？（09-11 报告在复核触发条件里留的那个口子） | **不成立，口子关掉，09-11 的原判（必须送审 + 标审图号）维持。** 《地图审核管理规定》**第六条**三项免审情形逐字取回后逐条对照，**一项都套不上**：（一）要求「直接使用自然资源主管部门提供的**具有审图号的公益性地图**」而我们用的是高德商业底图；（二）「景区地图、街区地图、公共交通线路图等内容简单的地图」🔑 **是穷举三类载体不是「简单程度」的判据** —— 全国按城市聚合的分布图不属于其中任何一类；（三）要求「不涉及国界、边界、历史疆界、行政区域界线**或者范围**」🔑 **「或者范围」四个字是决定性的** —— 按城市聚合并定位这些城市就是在表示行政区域范围。⇒ 「只到城市级」降低的是**下钻深度**，不改变「它是一张全国分布图」这个性质。上位法《地图管理条例》那句「景区图、街区图、地铁线路图等**内容简单**的地图除外」，「内容简单」是**跟在三类载体后面的总结**不是可独立援引的宽泛标准 —— 把它单独摘出来当判据正是 09-11 留下那个口子的根源。🆕 **两条 09-11 没记的量化后果**：**第二十八条** 互联网地图服务审图号有效期**两年**、且申请人应当**每六个月**将新增标注内容向审核部门**备案**；**第三十二条** 与审核通过的内容/表现形式不一致或有效期届满未重新送审，可**责令改正、给予警告，处 3 万元以下罚款** ⇒ 这是每 6 个月复发的运维义务不是一次性动作，且全国图「主要表现地为两个以上省级行政区域」落在**自然资源部**这一档，地方办不了。🔑 **但「城市级」这个决定仍然是对的，只是理由要换** —— 支撑它的另两条理由不受影响且更硬：Strava 2018 泄露美军基地的机制（低基数区域聚合退化成逐人定位）让我们全国几十人规模**整体处在退化区** ⇒ 盲人的点**不得以个体粒度上图**、k-匿名须服务端执行；且盲人要的是「有人愿意帮我」不是「还有别的盲人」。⚠️ **必须连带记住，否则下一轮有人发现「免审不成立」会顺势把城市级也当失效前提退回个体粒度 —— 那直接踩隐私红线。** **对 UI 设计的直接影响**：画稿与合规**完全解耦**，现在就能设计，送审是上线前的关卡不是设计前的关卡；但**设计时就要把城市级聚合当硬约束**，画出带个体点位或街道下钻的稿改起来是重画。后端侧当场实测：`demo` 仓库 `origin/main` 的 `api_spec.yaml`(8064 行) 搜 `distribution|aggregat|heatmap|city|/stats` **0 命中** ⇒ 09-11 另一条触发条件「后端上线分布聚合端点」**未触发**。⚠️ 本篇是文本比对**不是法律意见**，主管部门实际定性口径可能更宽或更严（「卫星影像底图 + 自有标注」仍无官方定性）。🚩 重申一条独立成立的既有缺口：**全仓 0 处提及审图号而我们已在展示高德地图** | 《地图审核管理规定》或《地图管理条例》修订（尤其第六条三项与第二十八条期限）；自然资源部就「App 叠加自有点位」出台明确口径；产品改为「只显示用户周边」而非全国（09-13 已给判据：那时不需要审图号，但**那是另一个产品**不是本功能的收缩版）；产品改为只做对内演示不进 App（全部合规义务不适用）；拿到专业法律意见 | [map-review-exemption-ruling-20260921.md](./map-review-exemption-ruling-20260921.md) |
| 2026-09-06 | Opus 5 官方怎么用？effort 该设几档？社区评价与技巧？本仓库工作流哪里能优化、怎么减少幻觉/返工？ | **官方 Opus 5 页逐条点了社区在抱怨的坑，而我们的规则正好写反了两条**。① `~/.claude/CLAUDE.md` 的「明确告诉 reviewer 只报影响正确性的」在 Opus 5 上**会真的少报**（官方原文 "may follow that instruction literally and report less; ask it to report everything and filter in a separate pass"）→ 改两段式：全报+带严重度，第二遍再过滤；② effort 口径过时 —— 「整会话 `/effort xhigh`」是 **Opus 4.7/4.8** 的官方起点，**Opus 5 官方起点是 `high`**，且明说 carried-over 的 effort 要重扫（⚠️ 初稿写「`settings.json` 没设所以跑的是默认」**是错的**，实测明确设着 `"effortLevel": "high"` —— 探测脚本没打印这个键就断言不存在，同 `rtk-silently-truncates-long-tool-output` 的形状）；**且 §F 那张表已按姊妹篇的三个反向证据（system card "make more changes than the task requires" / Cognition 最佳分在 medium / CodeRabbit xhigh 召回反降）整体下调一档，`max` 从不用**；③ **常驻上下文实测 21,232 token**（53,082 字符／8 文件，`AGENTS.md` 9,058 + `MEMORY.md` 5,899 占大头），官方原文 "Bloated CLAUDE.md files cause Claude to ignore your actual instructions" —— 「说了不听」的第一嫌疑是规则太长不是模型；④ **全仓 0 条控制输出长度的指令**，而 Opus 5 天生输出更长且**降 effort 不会让它变短**（effort 管「想多少」不管「说多少」），官方给了逐字可抄的三段指令（简洁 / 播报节奏 / 更正克制）；⑤ Opus 5 **更爱派 subagent**，光靠 prompt 拦不住，本机 2.1.224 支持 `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS` / `..._SPAWN_DEPTH` 硬上限（当前未设，默认 20/3）；⑥ **官方要求删掉泛化自验指令**（"remove them... reduces wasted tokens with no loss in quality"）——⚠️ 射程只到「靠嘴说的自验」，本仓库的真机测试纪律与 6 个 hook 属确定性 gate，**不在内**；⑦ 纠正一条社区流传的错误：`ultrathink` **不改 API 的 effort 值**（官方原文 "The effort level sent to the API is unchanged"），所以「设了 max 再打 ultrathink 会降档」不成立；`ultracode` 是另一回事（xhigh + dynamic workflow 编排，且 `CLAUDE_CODE_EFFORT_LEVEL` 不接受它、设了非 xhigh 值会让它静默失效）。**已否决**：`CLAUDE_CODE_DISABLE_ADAPTIVE_THINKING=1`+`MAX_THINKING_TOKENS`（HN 04 月热门配方，Opus 5 上 `budget_tokens` 直接 400）、无脑 `/effort max`（官方 "prone to overthinking"）、全局禁 subagent（改用上限不用开关）、**把官方的范围收口/纠错克制/委派三段 prompt 抄进 CLAUDE.md**（`claude_code` 预设已内置，抄了是同一件事说两遍）。⚠️ **同题姊妹篇在后端仓库** `demo/docs/research/opus5-workflow-optimization-20260906.md`（同日 01:42，两边各查了一遍才发现）—— 它更强的两块是 effort 反向证据与「预设已内置哪些 prompt」，本份独有的是 Claude Code 侧机制、`/goal`、HN 原始评论与本仓库瘦身实测。**8 条落地已完成 7 条**（#6/#7 常驻 21,556→16,440 tok 见 PR #109；#1 因全局文件正被并发修改而留给使用者补） | Anthropic 发布 Opus 5 后继模型或改 Opus 5 的 prompting guide / effort 建议；Claude Code 改 `/effort`、`ultracode` 或 `CLAUDE_CODE_MAX_*` 语义；`AGENTS.md` / `MEMORY.md` 体积再涨（阈值：常驻超 25k token 重量一次）；我们真的换主力模型 | [opus5-workflow-and-effort-20260906.md](./opus5-workflow-and-effort-20260906.md) |  ⚠️ **2026-09-21 合入时补两条**：① §C.3（`/goal`）**已复核成立**，来源 URL 返回 200 —— 而这份报告被关在 PR #108 里正是 09-19 误判 `/goal` 的根因，见上面 09-19 那行；② 报告进度表里两处「✅ 本轮完成」**主线上都没有**（#6 `AGENTS.md` 迁 skill 写着「PR #109」而 #109 已关闭、main 仍 501 行；#8 `/goal` 写法写着「已写进 `aidrun-ship-check`」而 main 上该 skill 里 `/goal` 零命中）⇒ **调研报告里的「✅ 本轮完成」指「我在某个分支上做了」，不等于主线上有。**
| 2026-09-21 | `/goal` 好不好用？能不能拿它接本仓库真机测试的「跑到绿」？ | **机制对口，但有一条失控风险本仓库必然撞上** ⇒ 条件里带 `or stop after N turns` 在这里是**必需项不是保险**。官方唯一的失控保护是「**连续几轮没有工具调用**就停下」，而真机测试失败时**每轮都真的跑了命令** ⇒ **保护结构性失效**；叠加 `device-test.sh` 是**先探活的快速失败**（设备离线/锁屏立即返回，不等超时），「快失败→判 not yet met→再跑一轮」可以转得很快，而两台真机长期离线是本仓库常态（记忆 `ui-test-runner-needs-usb-not-wifi` 11 种签名大多要人动手）。推荐条件：`/goal scripts/device-test.sh 的输出里 failed=0 且 passed>0，且我没有改动 blindRunTests/ 以外的文件，or stop after 12 turns`—— `passed>0` 那半**不能省**，它是「零执行不是通过」，少了它**设备锁屏会被判成达成**。🔴 **本机 2.1.224 拿不到三个保护性特性**：check-in 需 **2.1.234+**、idle check-in 需 2.1.236+、自动重试提示需 2.1.269+ ⇒ 后台任务卡住时**不会有 check-in 来救**（顺带订正 `opus5-workflow-and-effort-20260906.md` §C.3：它写的「30 分钟 check-in + 1h→2h 退避」是文档机制，**本机版本拿不到**）。机制要点（官方逐字）：`/goal` **本身就是一个 session 级 prompt-based Stop hook** 的 wrapper；评估器是 small fast model（默认 Haiku）、返回 not yet met / met / impossible 三裁决且**不跑命令不读文件**（*"It doesn't run commands or read files independently"*）⇒ 条件必须写成脚本输出里真会出现的字样；条件上限 **4,000 字符**；`/goal` 看状态、`/goal clear` 清掉；**不改权限模式**（无人值守要配 auto mode）；评估成本官方称 *"typically negligible"*，⚠️ 但换评估模型只有 `ANTHROPIC_DEFAULT_HAIKU_MODEL` 而它**不止影响 /goal**（`haiku` 别名与会话摘要一起改）⇒ **别动它**。⚠️ **与本仓库 Stop 钩子 `stop-checklist.mjs`（exit 2 + stderr，`stop_hook_active` 兜底）的叠加仍未实测** —— 推断方向一致不冲突（阻止停止 ≠ 结束 turn），但官方把「a hook that ended the turn」列为 pause 诱因；最可能出问题的是**钩子回灌的 stderr 在对话里，评估器可能把「收尾没做完」读成条件未达成的理由**。⚠️ **社区评价不一致，且唯一具体的负评正撞本仓库最怕的形态**：HN `mohsen1`（2026-05-28，48313546）逐字 *"Claude Code /goal or even /loop does not work hard enough and gives up… just claiming it's 'iterating' in a broken loop"* —— 是 Claude Code 刚跟上该功能时（`/goal` 由 Codex 先有，redhale 48293046）的评价，但**它描述的失效形态就是「假绿」** ⇒ **`/goal` 报达成之后仍要自己看 result bundle 的 `passed=N failed=0`**，它换掉的是触发方式不是判据。已落地：写进 skill `aidrun-ship-check` §6。**本报告 0% 实测**（斜杠命令是用户的输入通道，我发不出去），三条待验见 §7 | 本机 Claude Code 升到 2.1.234+ / 2.1.269+（§4 那张表要重算，check-in 与自动重试届时生效）；真的跑过一次 `/goal`（§3 的核心风险与 §5 的叠加行为需要实测证实或推翻，整篇从「未证实」升级）；官方改评估器的失控保护判据或 `/goal` 与 Stop hook 的交互；本仓库拿到稳定的真机通道（§8 第 1 条的分诊前提就变了）；`stop-checklist.mjs` 的阻断方式从 exit 2 改成别的 | [goal-command-for-device-tests-20260921.md](./goal-command-for-device-tests-20260921.md) |
| 2026-09-23 | 「星火同行」陪伴地图 HTML 原型（以「你」为中心的 2 公里局部地图 + 即时请求陪跑 + 「听见星光」声音化）接回 iOS：数据从哪来、声音化外面怎么做、「身边有人」怎么展示、有没有即时匹配先例 | **原型 9 块里 4 块契约现成**（接驳金星=WS `VOLUNTEER_LOCATION_UPDATE` 精确坐标只给配对双方；今日足迹=`/orders/{id}/track`；认证；`standardNight` 深色底图），**3 块无端点**（身边 N 位志愿者 / 顶栏全局统计 / 志愿者看跑者点位 —— `AvailableOrderResponse` 刻意不给坐标），**2 块撞红线**（「请求陪跑 3 分钟到」违反 ≥30 分钟 + 通话磨合；「某地亮了起来」是 09-13 判过的时序攻击通道）。盲人端接驳阶段**实际看不到地图**（初稿误写「全仓零地图」，已在报告里订正）。声音化：最近先例 Microsoft Soundscape **前提是立体声耳机**、按需一次只念 4–5 个点；pitch+panning 映射有文献但几乎全用耳机，**外放效果与和 VoiceOver 并发的研究均未找到**。Uber 下单前只显示最近 8 辆且「某些情况叫车前不显示单车具体位置」。United In Stride 是目录+私信自约，**没找到任何即时派单的助盲跑先例** | 产品决定开放「现在就跑」（§5 规则改写）；后端上线附近志愿者聚合端点；拿到外放声像定位或与读屏并发的实测研究；VoiceVista/Soundscape 改交互 | [xinghuo-companion-map-20260923.md](./xinghuo-companion-map-20260923.md) |
| 2026-09-24 | 记录列表的陪跑员路线缩略图（≤64 点、GCJ-02）用高德截图还是纯 SwiftUI `Path`？ | 高德**没有**独立截图器：`takeSnapshotInRect` 与 `MAMapSnapshot` 都要一个已显示的 `MAMapView`（官方指南「只有内容先显示在地图上，才能进行截屏」），列表要离屏开地图、等瓦片、逐行截；`Path` 无网络依赖、可单测。负责人选 `Path`（高德截图放弃） | 高德 SDK 升级出现独立截图器；列表要显示底图地名 | [amap-snapshot-for-list-thumbnail-20260924.md](./amap-snapshot-for-list-thumbnail-20260924.md) |
| 2026-09-24 | 高德 iOS 能不能画「白色描边 + 按配速渐变」的路线？API 叫什么？ | 渐变有：`MAMultiPolyline` + `MAMultiColoredPolylineRenderer`（`gradient = YES`，颜色锚在 `drawStyleIndexes` 上插值；索引点不抽稀，要少放；连续重复点必须去重；只支持圆头圆角）。**描边没有 API**，只能在下面垫一条更宽的白色 `MAPolyline` | 高德 SDK 升级（现 11.1.200）；换 MapKit | [amap-gradient-polyline-and-outline-20260924.md](./amap-gradient-polyline-and-outline-20260924.md) |
