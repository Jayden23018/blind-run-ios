# 账户生命周期加固验证记录（2026-07-11）

## 客户端专项验证

- 会话恢复、无角色恢复、撤销会话清理、注销成功/401/失败/仅本机退出、幂等清理、用户级应急元数据清理、429 Header 优先级和注册限流倒计时：8 项单元测试通过。
- 无角色 UI 恢复、全部注销入口、撤销失败警告与本机退出、盲人账户删除、志愿者活动订单阻断和防重复提交：5 项 UI 测试通过。
- 人工验收：用户确认全部账户生命周期流程正常。

## 云端契约验证

- 外部云端地址仍为 `http://47.114.113.171`，本仓库未加入任何后端实现。
- 2026-07-11 决策：云端破坏性探针不得使用长期测试账号，只能使用获批的可销毁一次性手机号。
- 2026-07-11 首次执行 `scripts/auth-account-lifecycle-probe.mjs` 时发现注销会使同账户替换 Token 一并失效；该结果已反馈后端。后端随后确认预期为“仅撤销当前 Token”并完成修复，不需要修改 OpenAPI、OpenSpec 或 iOS。
- 2026-07-14 使用同一获批的一次性手机号重跑完整探针：当前 Token 注销后被 401 拒绝且另一有效 Token 不受影响；删除其他用户 ID 返回 403；活动订单删除返回 `409` 与 `ACTIVE_ORDER_ACCOUNT_DELETION_BLOCKED`；取消测试订单后软删除成功；该账户全部 Token 均返回 401；原手机号重新注册成功。
- 探针首次删除用户 `25`，重新注册生成用户 `26`；验证完成后用户 `26` 也已成功删除。此前中断产生的用户 `23`、`24` 及测试订单均已清理，无遗留测试账号或活动订单。

## 发布基线

- `111`：修正后的完整基线通过；20 个 UI 测试中 1 个云端 smoke 按 Debug 构建规则跳过，198 个单元测试中 1 个仓库文档用例因真机沙箱不可访问仓库而跳过，其余无失败。最终 `xcresult` 为 Passed、0 failures、2 skips。
- `iPad Pro (2)`：修正后的完整基线通过；20 个 UI 测试中 1 个云端 smoke 按 Debug 构建规则跳过，198 个单元测试中 1 个仓库文档用例因真机沙箱不可访问仓库而跳过，其余无失败。最终 `xcresult` 为 Passed、0 failures、2 skips。

## 2026-07-14 代码审查修正

- 已补充异常角色恢复清理、真实两阶段账户删除确认、活动订单阻断的 TTS/VoiceOver 公告，以及删除后重新请求短信验证码的云端探针顺序。
- Simulator `build-for-testing` 通过；新增异常角色与无障碍公告单元测试，账户删除 UI 测试已更新为验证两个确认阶段及最终操作及时消失。
- `111` 与 `iPad Pro (2)` 的专项回归均通过：2 个新增单元测试和 2 个账户删除 UI 测试无失败。
- 两台设备的最终完整基线均通过，0 failures、2 skips；跳过项分别为 Debug 构建下的云端 UI smoke 和真机沙箱中不可读取仓库文档的校验用例。
- 云端可用性单独记录：2026-07-14 的获批一次性手机号探针结果仍有效；审查修正后的探针脚本通过语法检查，但因该一次性手机号已销毁且未获批新的可销毁手机号，未再次执行破坏性云端探针。本次真机基线不把 Mock/Debug 结果当作云端发布签字。
