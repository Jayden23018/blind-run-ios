## 1. 色板

- [x] 1.1 `FlowPalette` 加状态色、`onHero*`、引导绳白色系、汇合暖色控件，gold 改 `#FFD978`；删 `onNavy*` / `volunteerDot` / `runnerDot` / `ropeOnNavy` / `mutedOnNavy` / `mutedStrokeOnNavy`
- [x] 1.2 `FlowDesignSystemTests` 改为按状态色逐个验对比度，并加一条验红（交付包原值 80% 白压完成绿不过 4.5）

## 2. 头卡与引导绳

- [x] 2.1 `VolunteerOrderHero.Style` 按阶段给状态色；`FlowHeroCard` 接状态色并做 0.35 秒过渡
- [x] 2.2 `RopeView` 彩色头卡主题（陪跑员白色实心 + 状态色姓氏、跑者白描边、绳子 75% 白）；出发中 ETA 更新 easeOut 0.6 秒
- [x] 2.3 完成页并肩插图按 C03 配色，底为完成绿；进入时插图放大淡入、金色绳画出

## 3. 汇合页

- [x] 3.1 方位盘扇形 / 箭头 / 圆心 / 投影改琥珀
- [x] 3.2 响铃按钮改暖色底 + 描边 + 深琥珀字；④b「打电话」改琥珀底白字

## 4. 动画与触感

- [x] 4.1 页面按阶段切换走 spring（减弱动态效果时 0.2 秒淡入淡出）；方位盘 0.9 倍缩放淡入；提醒条在减弱动态效果下不位移
- [x] 4.2 ②→②b 主按钮原地升级（0.98→1 缩放 + 交叉淡入）
- [x] 4.3 接下邀请成功 `.success` 触感

## 5. 文档与验证

- [x] 5.1 `docs/ui/design-direction.md` §6.1 加状态色例外（V13）
- [x] 5.2 Preview：①–⑤、④ 五个距离档位、④b、跑者取消 v1 已齐；彩色头卡配色随组件生效。⑤ 有留言 / 无留言依赖完成页留言卡，v1 没做，另立 #228
- [ ] 5.3 真机跑覆盖改动的 suite，截图对照画板
